# Terry - Haley Yachts Website Engineering Task List

> **Owner / sole writer: Terry.** This is the only Haley-website task file Terry edits.
> William rolls this up into `docs/TASK-LIST.md` (master). Do not edit the master directly.
> **Haley Yachts website engineering ONLY** (haleyyachts.com: articles / Article Manager, the Featured Yacht editor, listing/yacht detail pages, SEO / sitemap / JSON-LD, image + video optimization, email templates, the floridayachts.ai redirect, address/footer, brand-art, the marketing site). **YBS (Yacht Broker Support / the Broker Suite) engineering now lives in the YBS repo** at `yacht-broker-support/docs/tasks/terry.md`, split out 2026-07-17 per Clark's standing rule that YBS and Haley Yachts are separate projects and must not commingle.

*Last updated: July 17, 2026 (split YBS engineering out to the yacht-broker-support repo per Clark's separate-projects rule; this list is Haley-website only now). Prior Haley-website history: Riviera 4300 spotlight article PUBLISHED via Article Manager - DONE; MLS feed integration CLOSED/not-actionable - OWYG feeds listings to the MLS centrally, no per-broker feed exists; Leadfeeder trial DROPPED - low value for a consumer brokerage. Prior - floridayachts.ai RESOLVED: Clark set a clean Permanent 301 to haleyyachts.com in GoDaddy, no masking; verified live (301 -> haleyyachts.com, HTTPS fixed). Decision: haleyyachts.com primary, floridayachts.ai 301s in, floridayachts.com unavailable. Prior - DUPLICATE BRAND-ART DIRECTORY REMOVED: Clark approved "get rid of the duplicate brand art." Re-verified `logo-haley-yachts-logo/` and `images/brand/favicon_logo/` are still byte-identical (all 6 md5s match pairwise + `diff -r` exit 0) and that NEITHER is referenced in any served/config file (HTML/CSS/JS/JSON/XML/manifest/txt/htaccess) - only internal docs mention the path, and they point at the keeper. Removed the redundant `logo-haley-yachts-logo/` via `git rm -r`, kept `images/brand/favicon_logo/` (the documented canonical location in `images/brand/INDEX.md` + logo-rebuild brief). Freed 11,755,281 bytes (~11.2 MB); NOTE my earlier "~22MB each" flag overstated it - actual is ~11.7MB per dir since the two .eps are ~5.5MB each. Post-delete grep = 0 broken refs. Commit 8c1847d, sync-push clean (4bed360..8c1847d). Recoverable from git history. Prior - ORPHAN/DUPLICATE IMAGE CLEANUP: Clark approved removing unused/duplicate photos. Deleted 5 unreferenced orphans via git rm = ~19.8 MB: travel/clark-haley-headshot.jpg (10.25MB dup), southern-wind.original.jpg (7.93MB pre-overlay master), yacht-overhead.jpeg, the non-square clark-lean-crop, and 360/kuula-card copy.png. Post-delete grep = 0 broken refs. KEPT intentional off-site/future-use assets (social-media/cta-cards, images/email/**, 360 canonical cards, BBP_2820-2.jpg, the PNG favicon set, a few stock yacht jpgs). FLAGGED a byte-identical duplicate brand-asset DIRECTORY for Clark (logo-haley-yachts-logo vs images/brand/favicon_logo, ~44MB combined of source .ai/.eps/.pdf) - not deleted on my own call. Prior - ADDRESS CORRECTION: prior pass used the WRONG street number + no suite. Clark confirmed canonical = 2401 PGA Blvd, Suite 164, Palm Beach Gardens, FL 33410. Fixed streetAddress -> "2401 PGA Blvd, Suite 164" + geo -> 26.8430/-80.0738 in the footer partial, propagated to all 28 public pages via sync-footer.sh; updated the 6 displayed full-address strings (contact, valuation, privacy, 3 yacht pages). Repo-wide grep confirms ZERO "2601" remaining and one byte-identical string everywhere. Prior - Full repo-wide IMAGE OPTIMIZATION pass complete - 84 raster images resized/recompressed in place at role-appropriate dimensions + JPEG q80-82, same filenames so zero refs broke; ~125 MB saved (148 MB -> 23 MB across the touched files). rendezvous1.png converted to .jpg (1.1MB->139KB, one HTML ref updated). Two orphan masters FLAGGED not touched: articles/travel/images/clark-haley-headshot.jpg (10.5MB, unreferenced dup) and images/yachts/featured/southern-wind.original.jpg (7.9MB, intentional backup). Hero MP4 (14MB) still OUT OF SCOPE - needs a separate re-encode. Prior - headshots: Resized the two oversized headshots in images/people/ in place - clark-haley-headshot.jpg ~10MB -> 119KB and BBP_2820-2.jpg ~9.4MB -> 123KB. Prior: Deleted orphan duplicate Bahamas-permit article `...-changes-again.html`, Clark approved - keeper is the `-april-2026` file registered in articles-data.js + sitemap; plain delete, no dangling refs. Prior: Technical SEO tightening batch shipped: sitemap.xml + robots.txt, sitewide LocalBusiness JSON-LD, canonical/OG/Twitter on all money + listing pages, Product JSON-LD on the 3 yachts, cheap CWV wins. floridayachts.ai forwarding investigated - it is a masked GoDaddy forward, NOT a 301; redirect steps handed to Clark via William.*

## OPEN

### Fortunato price reduction: $395,000 -> $329,000 - DONE 2026-07-21 (needs cPanel pull)
Clark: "lower the price of the 1991 Southern Wind 72 to $329,000, every place on the website." Changed on every live, forward-facing surface. `yachts/fortunato.html` (8 spots: `<title>`, meta description, `og:title`, `product:price:amount`, `twitter:title`, the Product JSON-LD `offers.price`, `.listing-price`, and the Asking spec row - note the prior $435k->$395k pass only caught 5, the machine-readable price fields (og product price, twitter:title, JSON-LD) were left stale then, so they were carrying $395,000 while the visible page said $395,000 too; all 8 are now consistent at $329,000). `js/featured-yachts.js` (Fortunato card `name`, which feeds the homepage + buy.html featured grid). `social-media/cta-cards/render-360-cards.py` (both `sub_line` strings) and the two rendered Fortunato PNGs re-generated with Pillow 11.3.0 - visually verified the 1350 card reads "Bruce Farr Fast 72 | $329,000". The shared render run re-encodes the Fringe Benefits cards byte-wise with identical content, so those two were restored via `git checkout` to keep the diff honest (same handling as the prior pass). DELIBERATELY LEFT UNCHANGED: the dated June 2026 Logbook archives (`email-templates/issues/logbook-2026-06.html` and `articles/newsletters/2026-06-16-the-logbook-june-2026.html`) - sent/published historical records that reflect the price at send time; rewriting them would falsify a dated issue. Repo-wide grep outside `docs/` confirms those two archives are the only remaining `$395,000` strings. No other yacht's price touched.

### Fortunato brochure PDF price patched in place: $395,000 -> $329,000 - DONE 2026-07-21 (needs cPanel pull)
Follow-up to the price reduction above - Clark spotted the linked spec-sheet PDF still reading 395. It IS editable, no re-upload needed. `documents/yachts/featured/1991-Southern_Wind-Bruce_Farr_Fast_72.pdf` is the OWYG-generated 28-page brochure and its price is real extractable text (Type3 font, 2 occurrences, both on page 1: the 13.5pt header line under the title, and the 10.5pt "Price:" row in the Boat Details table). Patched with PyMuPDF 1.26.5: redact the two spans, re-draw `$329,000` at the identical origin/size in Helvetica-Bold, ink color sampled from the original glyphs at 300dpi = RGB(45,81,114). Digit metrics match closely (Helvetica-Bold renders the 8-char string at 56.3pt vs the original 55.92pt), so the header line does not reflow. VERIFIED: page 1 re-rendered and eyeballed against the before image - identical apart from the price; all 28 pages still present; a page-by-page text diff of old-vs-new shows page 1 as the ONLY page that differs, and only by the price swap; zero `395,000` strings left in the file. Method is reusable for the next reduction.

Clark confirmed he has already asked OWYG to update the price at the source - noted because this brochure is auto-generated by One Water's listing system, so until they do, the syndicated surfaces (YachtWorld / the MLS) keep showing the old number and any brochure regeneration silently reverts this patch.

### All remaining stale-price PDFs corrected + a reusable patcher - DONE 2026-07-21 (needs cPanel pull)
Clark's call: "leave the old Logbooks alone, but update any old pdf documents that are in our files." So the dated Logbook TEXT stays historical, while the PDF *documents* get brought current.

**Audited every PDF** under `documents/` (11 files) for dollar amounts and compared each against the live price in `js/featured-yachts.js`. Result: only the two leftover Southern Wind brochures were stale. Everything else already matches - Riviera 545 $1,295,000, Sunseeker Manhattan 68 $3,995,000, Sunseeker Superhawk 55 $1,975,000, Offshore 48 "Island Girl" $330,000, Riviera 64 $3,299,000, Riviera 465 $1,549,000; the HCB 56 brochure carries no price at all.

Patched both to $329,000, same technique and same verification as the primary brochure:
- `1991-southern-wind-bruce-farr-fast-72.pdf` (lowercase, 28pp) - was **$435,000**. This is the one the published May 2026 Logbook archive links to, so it was live and handing visitors a stale brochure.
- `1991-southern-wind-bruce-farr-fast-72.old.pdf` (30pp) - was **$495,000**. Not referenced by anything; corrected anyway per Clark's instruction. **Deletion candidate** - it is a superseded brochure with no inbound links.
Verified both: page count unchanged (28 and 30), page-by-page text diff against the committed originals shows page 1 as the only page that differs and only by the price, page 1 re-rendered and eyeballed, zero old-price strings remaining.

**New tool: `scripts/patch-pdf-price.py`.** Generalizes the method so the next reduction is one command instead of an ad-hoc script:
`python3 scripts/patch-pdf-price.py <pdf> '$395,000' '$329,000'` (`--dry-run` to preview). It finds every occurrence, samples the ink color from the original glyphs at 300dpi, redacts, redraws in Helvetica-Bold at the identical origin/size, warns if the redrawn width shifts more than 2pt (a digit-count change could reflow the header), saves via a temp file, and reports any leftover occurrences. Requires `pip install pymupdf`. Docstring carries the OWYG-regeneration caveat.

### Riviera 64 brochure link FIXED - DONE 2026-07-21 (needs cPanel pull)
Clark approved. `js/featured-yachts.js` slot 5 `pdf:` now points at `documents/yachts/featured/2023-Riviera-64_Sports_Motor_Yacht.pdf` (verified page 1 reads "64 ft 2023 Riviera 64 Sports Motor Yacht / $3,299,000 / Miami, FL", matching the card and the PDF audit). Was pointing at the HCB 56 brochure. STILL OPEN: the Riviera 64 and HCB 56 cards share the same `youtube` id `asIe1cVN4IE` - same copy-paste, but I cannot tell from here which boat the video actually shows, so Clark needs to confirm which card is wrong. NOTE the fix is on disk in the repo; if Clark has the Featured Yacht editor open it will write from ITS loaded copy, so he should reload the tool rather than save over this.

### CONFIRMED HAZARD - dropping a featured yacht can 404 a past Logbook's spec-sheet link
Clark raised this and he is right; I traced the code. In `admin/featured-yachts.html`, `deleteLoadedImpl()` gathers the entry's image and PDF and decides what to delete using `countAssetRefs()` (line ~1369), which counts references **only inside `workingData`** - that is, only the other featured slots. So an asset is treated as "unshared, safe to delete" even when a published Logbook archive, newsletter, or article links to it. The existing inbound-link scan (`scanInboundLinks`) does NOT cover this: it only runs for the entry's detail `page:`, never for the image or PDF.

Net effect: drop a boat from Featured, and any past Logbook that linked its brochure silently gets a dead link. We got lucky so far only because the May 2026 Logbook links the lowercase Southern Wind PDF, which happens to still be on disk.

**BUILT 2026-07-21 (needs cPanel pull)** - Clark approved both parts; he also confirmed he is not in the Featured Yacht editor until next month, so there was no rush and no risk of colliding with a live edit. What shipped, in `admin/featured-yachts.html`:

1. **Brochure PDFs are never auto-deleted.** `entry.pdf` is no longer a deletion candidate at all - it is captured as `keptDoc`, reported in the confirmation as "documents are never auto-deleted", and echoed in the success banner. Images still follow the old unshared/shared rule (card-specific and cheap to re-cut); detail pages still delete as before. Removing a brochure is now a deliberate manual act.
2. **`scanInboundLinks()` generalized from "the detail page" to "any repo file", and widened.** Three defects fixed at once:
   - it now runs over the **assets** (image + PDF), not just the detail page, so the dialog can see what `countAssetRefs()` structurally cannot;
   - it now walks **`articles/**` and `email-templates/**` recursively** (new `LINK_SCAN_DIRS`, depth-capped at 3), not just the top level and `yachts/` - the published Logbook archives live in `articles/newsletters/`, so the old scan was blind to exactly the pages we were worried about;
   - the href regex now tolerates a trailing **`?query`/`#fragment`**, because every newsletter link carries UTM parameters and the old pattern required a closing quote immediately after the filename, so it would have missed them even if it had looked in the right folder.
   Results are de-duplicated, per-file, and every deleted file that still has inbound links gets its own warning block in the confirm dialog and in the success banner.
Also corrected the Delete section's on-screen blurb, which claimed "Detail pages are never deleted" - the code has always deleted them.

**Verified** by extracting the real `scanInboundLinks` out of the page and running it against the actual repo under a `FileSystemDirectoryHandle` shim (bun), old version vs new:

| target | old scan | new scan |
|---|---|---|
| `1991-southern-wind-bruce-farr-fast-72.pdf` (May Logbook's brochure) | **(nothing)** | May Logbook archive + May email issue |
| `yachts/fortunato.html` (detail page) | **(nothing)** | June Logbook archive + June email issue |
| `images/.../southern-wind.jpg` | fortunato.html | fortunato.html + 4 Logbook/email pages |

So the hazard was worse than the original diagnosis: it was not only the brochure. **Deleting Fortunato from Featured today would have removed `yachts/fortunato.html` and dangled two published June Logbook pages, while the confirmation dialog reported zero inbound links.** Both are now caught before the delete runs. Inline JS syntax-checked clean.

Part 3 (the canonical-document convention) is BUILT too - see the next entry.

### Featured slots 6 -> 12 - DONE 2026-07-21 (needs cPanel pull)
Clark: "why don't we just add another 6 slots on the featured listings? Since I still don't have the worldwide search up and running." So the featured grid carries more inventory while search is missing. This also answers the Island Girl question - she no longer has to displace anyone.

**One constant, not fourteen literals.** The count was hardcoded in ~14 spots in `admin/featured-yachts.html` (pad loops, `slice(0, 6)` trims, the compaction re-pad, the data-file writer, an error string) plus two hand-written `<option value="1..6">` lists. Replaced all of it with a single `const FEATURED_SLOTS = 12` at the top of the admin script; both Position dropdowns are now generated from it at load, so the markup carries no slot count at all. Changing the number again is a one-line edit.

**The public grid needed nothing** - `renderFeaturedYachts()` filters on `isFilledYacht` and never had a cap, so empty slots stay hidden and filled ones just render.

**Home page capped at 6, deliberately.** Not asked for, but adding 6 slots would otherwise have doubled the home page's featured section the moment Clark filled them, and that section already ends in a "View All Featured Yachts" button - it was built as a preview. `renderFeaturedYachts(containerId, limit)` now takes an optional cap, and a page opts in with `data-featured-limit` on the grid div. `index.html` sets 6; `buy.html` passes nothing and shows everything. Net effect: the home page looks exactly as it does today no matter how many slots get filled, and the Buy page is the full list. Trivially reversible by deleting one attribute.

**Verified** by running the real `js/featured-yachts.js` under a DOM shim (bun): with today's 6 filled boats, home renders 6 and Buy renders 6; with all 12 filled, home renders 6 and Buy renders 12. Separately simulated the admin pad/trim/compaction against the live data file - 6 entries pad to 12, 6 filled + 6 hidden blanks, compaction keeps filled first and blanks last, and the dropdowns emit 1-12. Admin inline JS parses clean.

Also refreshed the admin version stamp and corrected `admin/index.html`, which still advertised "up to six featured yachts".

### Island Girl listing page BUILT + tidiness decisions applied - DONE 2026-07-21 (needs cPanel pull)
Clark's four calls on the audit: (1) build the Island Girl page, (2) delete the test email heroes, (3) commit the brand-archive deletions, (4) leave the newsletter/site image duplicates alone.

**(1) `yachts/offshore-48-island-girl.html` now exists.** This closes TWO live dead links, not one - `sitemap.xml` was advertising the page to Google, and the **published June 2026 Logbook archive links straight to it** (`href="../../yachts/offshore-48-island-girl.html"`), as does the June email issue. So we had been sending both Google and newsletter readers to a 404 since June 16.
Built from `yachts/fringe-benefits.html` (the only detail page with a real multi-photo gallery + lightbox), then adapted:
- All 15 `images/yachts/island-girl/gallery/*.jpg` wired into the grid, `islandgirl-hero.jpg` as hero and OG/Twitter image. These assets were already in the repo, unreferenced - now they earn their keep.
- Specs and narrative written from the actual brochure: twin Caterpillar 3126-TA 420hp (installed 2006, ~2,000 hrs), 15'6" beam, 4'2" draft, 2 staterooms / 2 heads, 600 gal fuel / 240 gal water, new batteries + Victron inverter/charger 2026, 8kw Northern Lights genset, Sleipner bow thruster, Garmin electronics. Asking $330,000, lying Palm Beach Gardens.
- Title/meta/canonical/OG/Twitter/Product JSON-LD all rewritten for this boat; price `330000` in both the OG product tag and the JSON-LD offer.
- Removed the inherited 360 Walkthrough section (no Kuula tour for this boat) and the Watch Video lightbox + its button (no video), so there are no dead controls.
- Renamed the copied `fb-*` / `fbGallery*` CSS classes and JS handlers to `gal-*` / `galClose` / `galStep` so nothing in this page reads as another boat's markup.
**Verified:** every local asset ref resolves, the single inline JS block parses (bun), both JSON-LD blocks parse and validate as Product + LocalBusiness, every `onclick` handler has a matching definition, and site-wide there are now **0 broken internal page links and 0 dead sitemap URLs**.
NOT DONE, needs Clark: Island Girl is not in `js/featured-yachts.js` - all 6 featured slots are full, so adding her means displacing another boat. His call.

**(2) Test email heroes deleted.** `2026-05-25-2231/2232-test-of-the-subject-line.jpg` removed. **HELD BACK the other two** in `images/email/hero/` - `2026-05-26-2054-take-the-360-tour-of-fortunato.png` and `2026-05-26-2119-virtual-tour-2020-riviera-545-suv.png`. They are named for real campaigns, and `admin/email-composer.html` sets `HERO_URL_BASE = 'https://haleyyachts.com/images/email/hero/'` - composed emails reference their hero by ABSOLUTE URL and go out through Constant Contact. If those two sends happened, deleting the files breaks the hero image in mail already sitting in inboxes. Same failure mode as the Sangaris brochure. Asked Clark to confirm before removing; my "junk" framing when I first flagged them was wrong.

**(3)** The 8 archived logo concepts/monograms under `images/brand/archive/` committed as deletions (they had been sitting deleted-but-unstaged in the working tree since before this session; recoverable from history). Working tree is finally clean.

**(4)** ~3MB of byte-identical duplicates left in place by decision - `articles/newsletters/images/` keeping its own copies of site photos is deliberate isolation, so a future site-image cleanup cannot break a sent issue.

### Fortunato email hero, revision 2 (Clark's layout notes) - DONE 2026-07-21 (needs cPanel pull)
Four changes off Clark's marked-up screenshot, all in `social-media/cta-cards/render-360-cards.py` as reusable options rather than one-off tweaks:
1. **Title pinned to the top** (`title_at_top=True`). Eyebrow + hull name move to y150/y222; the accent rule stays welded under the headline. Everything below (banner, price, teaser, chip) now flows from the BOTTOM OF THE PHOTO rather than from the headline, otherwise it would land on the boat.
2. **Inverted top scrim** (`top_scrim=620`). Solid navy at y=0 easing down on an ease-out curve - the mirror of the existing bottom fade. Gives the headline something to sit on and pushes the superyacht behind Fortunato back, which is what Clark wanted it for.
3. **Bottom fade no longer eats the hull** (`photo_height=1150`, `bottom_fade=110`). The fade was a fixed 220px and was swallowing the bottom of her hull including the red boot stripe; the photo strip is taller and the fade much shorter, so she sits fully in frame.
4. **Stacked co-brand** (`cobrand=True`, new `place_cobrand_bottom_corner()`). One Water reverse banner ABOVE the Haley mark, One Water slightly the larger of the two (270x76 vs 230x62), both aligned to the same edge, matching the website and email footers. Returns the whole stack's rect so the contact strip centres against the pair.

**Regression-tested, because these options touch shared code that renders the social cards.** Rendered all four existing cards with the new script and compared pixel-by-pixel against the OLD script's output: **identical on all four**. One trap caught in the process - my first pass scaled `bottom_fade` by the canvas ratio, which silently changed both 4:5 renders; the fade was historically a fixed 220px on every aspect, so it is now deliberately NOT scaled and that is commented in the code.

**Pre-existing, NOT caused by this change:** the two committed `fringe-benefits-360-*.png` files no longer match what the script produces from the current `riviera545suv.jpg` - they differ only in the top strip (114px at 9:16, 20px at 4:5), i.e. the baked PRICE REDUCTION banner area. The committed cards predate the last re-bake of that photo. Verified by rendering with the ORIGINAL script straight out of git and diffing against the committed PNGs. Harmless (they are social assets, not served by the site) but they are stale - re-render when Clark next wants those posted.

### Repo tidiness pass - part 1 (safe renames) - DONE 2026-07-21 (needs cPanel pull)
Clark: "make sure we're as organized as we can be with folders." Ran a full repo audit (549 tracked files, 426 assets); findings and the open decisions are in the entry below this one. This entry is what was safe to just fix.

**Featured hero images renamed off their camera/MLS export filenames.** Three cards were using `screenshot-2026-07-13-at-8-11-40-pm.png` style names, including the hero for a $2.9M listing, and the Manhattan was on a 60-character MLS export string:
- `screenshot-2026-07-13-at-8-11-40-pm.png` -> `2024-sunseeker-superhawk-55.png`
- `screenshot-2026-07-13-at-9-06-00-pm.png` -> `2023-riviera-64-sports-motor-yacht.png`
- `screenshot-2026-07-13-at-8-28-19-pm.png` -> `2026-hcb-56-suenos-gix.png`
- `2024-sunseeker-manhattan-68-power-10205500-20260710104638123-7.jpg` -> `2024-sunseeker-manhattan-68.jpg`
Now matching the brochure naming convention, so a boat's hero and her spec sheet share a stem. Two of the four appear in the **already-sent July 2026 Logbook**, so those old URLs got `RedirectPermanent` lines alongside the brochure ones - same rule as the documents: a published URL has to keep resolving.

**Riviera 64 video cleared.** Clark confirmed the video on that card is the HCB's, so `youtube` is now empty on the Riviera 64 and stays only on the HCB 56. Closes the leftover from the brochure fix.

**Verified:** repo-wide scan of every local `href`/`src` asset reference - zero unresolvable, counting redirects. The only hits are `articles/_template.html` and the generated-page template inside `admin/article-manager.html`, which correctly use `../../` because the pages they emit live two levels deep; they are false positives, not broken links.

### Canonical per-boat brochure convention - DONE 2026-07-21 (needs cPanel pull)
Clark: "might as well go ahead and get that done." Part 3 of the document-retention work.

**New layout.** Brochures moved `documents/yachts/featured/` -> `documents/yachts/`, one canonical document per boat, named `<year>-<builder>-<model>-<boat-name>.pdf`, all lowercase and hyphenated (the old set was a mess of underscores, mixed case, and one filename containing a literal space and parentheses). Convention written up in `documents/yachts/README.md`, which is blocked from public serving by the root `.htaccess` `\.md$` rule. The 8 canonical brochures now sit flat in `documents/yachts/`; superseded revisions and secondary documents moved to `documents/yachts/archive/` (never linked, kept so an old revision does not require digging through git).

**Old URLs kept alive with 301s.** New section 3b in the root `.htaccess`. The already-sent Logbook issues cannot be revised, so a brochure's published URL has to resolve forever; the dated archives are therefore NOT edited to point at the new paths, exactly as Clark asked. The two 1991 Southern Wind entries both land on the current Fortunato brochure - the May issue linked an earlier revision, and someone clicking a spec sheet should get the accurate one (both read $329,000 now anyway).

**Admin tool follows the convention.** `admin/featured-yachts.html` uploads to `documents/yachts/` in both GitHub and local-folder modes (`copyPdfToFeatured` renamed to `copyPdfToDocuments`), and the two on-screen help strings and the schema comments were corrected.

**THE HAZARD WAS ALREADY REAL - found the live casualty.** While auditing which old paths needed redirects, one referenced file turned out not to exist at all: `2005-manta-42-mkii.pdf`. Traced it to commit **`3e968ce` (2026-06-08), "Featured yacht: delete asset documents/yachts/featured/2005-manta-42-mkii.pdf"**. Sangaris sold, came out of the featured rotation, and the delete path took her brochure with it - breaking the spec-sheet link in the **already-sent May 2026 Logbook** and in its published web archive. It sat broken for six weeks and nobody noticed, which is the whole problem with this failure mode. Recovered the PDF from git history (`git show 3e968ce^:...`), filed it as `documents/yachts/2005-manta-42-mkii-sangaris.pdf`, and added a redirect for the old path. That live dead link is now repaired.

**Verified:** every `pdf:` in `featured-yachts.js` and every spec-sheet href in `yachts/*.html` resolves to a file that exists; every old path still referenced anywhere on the site has a matching `RedirectPermanent`; every redirect target exists on disk; all moves recorded as git renames (no content churn); admin inline JS parses clean.

**The recommendation as originally written:**
1. **Never auto-delete PDFs.** Brochures are durable documents that get linked from dated archives; featured cards are ephemeral. Images can stay in the current delete flow (they are card-specific and cheap to re-cut). Cheapest possible change, removes the whole class of bug, and the cost is a few MB of orphan PDFs that a periodic cleanup pass can sweep.
2. **Make the confirmation dialog tell the truth.** Extend `scanInboundLinks` to run over the image and PDF paths too, not just the detail page, and list any inbound links in the confirmation. Then a delete can never quietly break a link Clark did not know about.
On top of that, Clark's own proposal - ONE canonical brochure per boat, referenced by both the featured card and the Logbook - is the right convention. Suggested home: `documents/yachts/<boat-slug>.pdf`, promoted out of `featured/` so the directory name stops implying the featured tool owns the file's lifecycle. That plus (1) means the document outlives the featured slot by design instead of by luck.

### BUG FOUND, now fixed above - wrong brochure on the Riviera 64 card
Surfaced during the PDF audit. In `js/featured-yachts.js` the **2023 Riviera 64 Sports Motor Yacht** card (slot 5) has `pdf: "documents/yachts/featured/2026-HCB-56_SUENOS_GIX.pdf"` - the HCB 56's brochure. Its correct file, `2023-Riviera-64_Sports_Motor_Yacht.pdf`, is sitting in the same folder unreferenced. The Riviera 64 and HCB 56 cards also share the same `youtube` id (`asIe1cVN4IE`), which looks like the same copy-paste. Net effect: anyone downloading the spec sheet for the Riviera 64 gets a different boat. Left alone pending Clark - this file is also written by the Featured Yacht browser tool, so the fix should land in whichever place he prefers.

### Featured Yacht editor - delete no longer shows a red "failed" on a card whose assets are already gone - DONE 2026-07-13 (needs cPanel pull)
Follow-up to 1344b6b. Reported symptom: deleting FY-3 (2018 Absolute 60 Fly, whose image + PDF were already removed from the repo) "still fails." Traced it: the delete path was actually already non-fatal for a missing asset (`ghDeleteFile` and `deleteFileLocal` both no-op on 404 / a missing file, and the data-array removal runs FIRST), so the card WAS being removed - the browser tool had in fact already deleted the Absolute card from `js/featured-yachts.js` (commit cbc0009) plus its assets. The real defect was the STATUS banner: any secondary asset/page cleanup hiccup flipped the whole banner to red `error` (`failures.length ? 'error' : 'success'`), so a successful card removal read as a failed delete. Fix: (1) `admin/js/github-api.js` `ghDeleteFile` now returns true/false (deleted vs already-absent) and also treats a 404 on the DELETE itself as already-gone - never throws on a missing file; (2) `admin/featured-yachts.html` `deleteFileLocal` returns the same true/false; (3) the delete handler counts removed vs already-gone assets, and because the card is out of the data file by the time cleanup runs, the banner ALWAYS reads success - leftover-file problems are a trailing note, not a red failure. Reorder (`moveToPosition`) and save (`saveFile`) confirmed untouched by the missing-asset case: reorder only patches the data file, save only UPLOADS an asset when a new file is staged. Data already correct on origin/main (Riviera pos 1, Southern Wind pos 2, positions 3-6 empty; no Absolute, no Sunseeker), so no data edit was needed. Commit <pending>; Clark must pull the admin code change to get the handler fix.

### OWYG co-brand in header - HOME PAGE ONLY (review-first) - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first. Review-first change: Clark wants the OWYG co-brand on the home-page header only, then he confirms before we roll it to the other 27 pages. Scoped strictly to `index.html` + new `.owyg-*` classes in shared `css/styles.css` (no existing shared rule changed in a breaking way; the new classes only exist on index, so adding them to the shared sheet is safe).
- [x] **Markup (index.html, before the hamburger):** added `<a href="https://owyg.com" target="_blank" rel="noopener" class="owyg-cobrand">` wrapping `<span class="owyg-cobrand-label">Yacht Broker with</span>` stacked above `<img src="images/email/owyg-banner.jpg" alt="One Water Yacht Group" class="owyg-cobrand-img">`. Used the light-bg banner JPG (header is white), not the -reverse version. Exact phrase "Yacht Broker with" per Clark.
- [x] **CSS (css/styles.css):** new `.owyg-cobrand` (column flex, right-aligned via margin-left:auto so it pins right when the contact strip is hidden), `.owyg-cobrand-label` (Open Sans, 0.62rem, 600, uppercase, 1.5px tracking, #555), `.owyg-cobrand-img` height 54px. HYS `.logo-img` is 48px, so OWYG reads slightly larger (54 vs 48) to balance the wide banner aspect, mirroring the email co-brand bump.
- [x] **Header height:** left global `.nav-container` at 72px (shared across all pages - did NOT touch it). The 54px logo + 3px gap + small label (~65px total) centers cleanly in 72px. Breadcrumb JS reads `header.offsetHeight` dynamically so no hardcoded offset breaks.
- [x] **Responsive:** `<=900px` (60px bar): hide the "Yacht Broker with" label, OWYG img -> 42px. `<=768px`: hide the centered `.header-contact` phone/email strip entirely so the two logos + hamburger never cramp; co-brand margin trimmed. Hamburger stays tappable, drawer unchanged. On phones OWYG ends up ~42px vs HYS 48px (slightly smaller) - acceptable tradeoff to keep the mobile header clean and non-overflowing per the brief.
- [x] **Confirmed scope:** ONLY index.html (markup) + styles.css (new classes) changed. No other .html page touched. Goes live only after Clark runs the cPanel pull (it's index.html, not Article-Manager content). Commit + sync-push below.

### Article Manager DRAFT LIFECYCLE - two bugs FIXED - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (origin in sync w/ ff09d2e; clean rebase, nothing behind). Clark reported two symptoms: (1) polished+PUBLISHED articles still showed in the Drafts list; (2) he recalled Patrick's "Exuma Cays" repo draft, edited it, hit Save Draft, reopened it, and got Patrick's ORIGINAL back (his edits vanished).
- **Root cause (single design flaw, NOT a localStorage-vs-repo split):** there is only ONE draft store - the repo `drafts/*.json` files via the GitHub Contents API. The bug is that the draft's FILE name was recomputed from the title via `buildSlug(title)` at every save/publish, with no stable identity carried on the recalled draft. Patrick's hand-committed file was named `exuma-cays-land-and-sea-park-cruising-the-exumas.json` but `buildSlug("Cruising the Exuma Cays Land and Sea Park: What We Get for Our Trouble")` = `cruising-the-exuma-cays-land-and-sea-park-what-we-get-for-ou` (colon stripped, truncated to 60). Mismatch.
  - Symptom 2: `saveDraft()` (admin/article-manager.html ~2441) wrote to `drafts/<buildSlug(title)>.json` = a NEW forked file; the original Patrick file the list showed was never touched, so recall returned his version. Edits went to a ghost file the list ignored.
  - Symptom 1: publish delete (`publishViaGitHub` ~2372) targeted `drafts/<buildSlug(title)>.json`, never the actual list file, so the published article's draft lingered forever.
- **Fix (minimal, draft-identity only - publish/articles-data.js untouched):** added state `currentDraftSlug`. `recallDraft` (~2518) now records the file's real on-disk slug from its path; `saveDraft` (~2441) overwrites THAT file in place (`currentDraftSlug || buildSlug(title)`), sets it after a new save, and refreshes the list; `publishViaGitHub` (~2372) now deletes BOTH the title-slug AND `currentDraftSlug`, then reloads the list so the item drops off immediately; `clearForm` + `deleteDraft` unbind `currentDraftSlug`. Both symptoms trace to the same fix: the list, Save, recall, and Publish-delete all key off one canonical slug.
- **Cleaned the existing stale state:** cross-checked all 10 repo drafts against `articles/articles-data.js`. THREE were already published but lingered (file name != published slug): `exuma-cays-land-and-sea-park-cruising-the-exumas.json`, `provisioning-crossing-to-the-bahamas.json`, `riviera-4300-sports-express-day-boat-bahamas-weekender.json`. Removed via `git rm`. The other 7 are genuine unpublished drafts - kept.
- **Verified locally:** static JS check (each touched function defined once, 11 `currentDraftSlug` refs consistent), buildSlug reproduced in Python to prove the mismatch, orphan detection script confirmed exactly the 3 stale files. **Could NOT verify in-browser** (needs Clark's GitHub token + the live tool). TEST FOR CLARK: open a repo draft -> edit -> Save Draft -> recall it = should now show your EDITS; publish any draft = it should LEAVE the Drafts list right after. Commit `6443df9`, sync-push clean (`ff09d2e..6443df9`).

### Articles page SEARCH upgraded - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (origin in sync w/ 2e71947; clean rebase, nothing behind). Clark wanted a SEARCH option on the Articles page so visitors can find articles. Found a basic search already wired in `articles/articles-data.js` (live filter on title/excerpt/keywords/author, debounced, plus the single-select category chips). Rather than rebuild, I extended it to meet the spec.
- [x] **Search box (articles.html):** replaced the plain inline-styled `#articleSearch` input with a styled field - magnifier SVG icon, accessible `<label>` + aria-label, and a clear (x) button (`#articleSearchClear`) that appears only while there is text. Sits directly above the category chips, above `#articlesGrid`. New CSS in `css/styles.css` (`.article-search-*`): cyan #21cbea icon + focus ring, navy #0a1628 text, pill border to match the chips, full-width on mobile (<=600px).
- [x] **Match logic (articles-data.js):** now searches title + excerpt + keywords + author + the category tag/label (so "travel", "boat review", "newsletter" match). Multi-word queries are AND (every whitespace-separated term must appear). Whitespace normalized/trimmed. Sort unchanged (newest first).
- [x] **Combines with the existing category chips** (AND), does not override - chip=travel + "Riviera" correctly returns 0 since Riviera reviews live in boat-reviews.
- [x] **Empty state** echoes the query: `No articles match "<query>".`, HTML-escaped (verified a `<script>` query is neutralized). Clearing the box (typing or the x) restores all.
- [x] **Did not touch the Article Manager schema** - search reads `publishedArticles` as-is; new entries flow in automatically.
- [x] **Verified (headless Chrome against the real files):** unfiltered 18/18; "Bahamas" 9, "Riviera" 6, "survey" 1; "Bahamas survey" -> 0 + quoted empty state; "travel" 4 (category match); author "Reshmi" 1; clear button toggles + restores 18. Commit `24b4dd0`, sync-push clean (`2e71947..24b4dd0`).
- FLAG (not built): the category chips already cover faceted browsing. A natural follow-on if Clark wants it: highlight the matched terms in the cards, or a result count ("12 articles"). Both trivial; left out pending a request.

### June Logbook newsletter published as a web article - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (origin had moved a lot today incl admin-tool publishes + the dock-card swap e93df0c; local was already in sync, clean). Clark: turn the June Logbook into a web ARTICLE (he handles the Constant Contact send himself), modeled on the May Logbook article precedent.
- [x] **Studied the May precedent** (`articles/newsletters/2026-05-09-the-logbook-may-2026.html` + its articles-data.js entry) and mirrored its structure, header/footer, meta/OG/Twitter/JSON-LD, and prose-reformatting of the email's table layout.
- [x] **Sourced CURRENT June content** from `email-templates/issues/logbook-2026-06.html` (post-corrections): Note from Clark (OWYG Bahamas Rendezvous, July 16-19, Freeport); 3 featured listings with CURRENT prices - Riviera 545 "Fringe Benefits" $1,295,000 (reduced from $1,495,000), Southern Wind 72 "Fortunato" $395,000, Offshore 48 "Island Girl" $330,000; the CURRENT 4 From-the-Dock articles incl the new Exumas piece (replaced the repeated Bahamas how-to); 4 recent sales (Manta 42 MkIII, Prestige F5.7, Sunseeker Predator 84, Riviera 6200 Sport Yacht).
- [x] **Built** `articles/newsletters/2026-06-16-the-logbook-june-2026.html` (live URL `https://haleyyachts.com/articles/newsletters/2026-06-16-the-logbook-june-2026.html`). Internal links are site-relative, no UTMs (those are for the emailed version only).
- [x] **Hero:** no June-specific hero existed in `articles/newsletters/images/` (May had `logbook-2026-05-hero.jpg`). Matched May's pattern by copying the issue's branded email masthead (`images/email/logbook-masthead.jpg`, 1200x720) to `articles/newsletters/images/logbook-2026-06-hero.jpg` and referencing it by bare filename so the Articles grid thumbnail (`articles/<type>/images/<heroImage>`) resolves. Sensible call, flagged here.
- [x] **Registered:** articles-data.js (newsletters bucket, above the May entry), sitemap.xml (above the May URL), and added to `scripts/sync-footer.sh` targets; ran sync-footer (footer + sitewide JSON-LD injected, 1 of 30 updated).
- [x] **Checks:** 0 em dashes / 0 "OWYS" / 0 "Denison" on the page; all relative links + the 2 root-absolute footer links resolve to real files; all 3 listing + 4 article targets confirmed present; HTML tags balanced; articles-data.js parses as 18 valid entries (Python json on the extracted array). Commit `b72edff`, sync-push clean (`e93df0c..b72edff`).

### PRICE REDUCTION banner clipped on LISTING pages (Fortunato hero + Fringe gallery) - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (clean, in sync w/ origin/main ff969ac; clean rebase, nothing behind). Clark: two more spots where the baked-in top banner gets clipped (top ~half cut), this time on the listing/detail pages not the featured cards - Fortunato (`yachts/fortunato.html`) HERO and Fringe Benefits (`yachts/fringe-benefits.html`) gallery FIRST tile. Same class of bug as the featured-card fix.
- [x] **Root cause confirmed (two shared rules, both `object-fit: cover` + default center crop):** (1) `.listing-hero-img` (css/styles.css ~1317) is `width:100%; height:auto; max-height:70vh; object-fit:cover`. southern-wind.jpg (1600x1067, ratio 1.50) at 1280 wide renders ~854px tall, over 70vh (~560px), so cover crops top/bottom centered and clips the top ~11.5% banner. (2) `.listing-gallery-grid img` (~1453) is a hard `aspect-ratio: 3/2` + `object-fit: cover`; riviera545suv.jpg (1600x1176, ratio 1.36, taller than 3:2) cover-crops to 3:2 centered, clipping the top banner.
- [x] **Fix:** added `object-position: top` to BOTH rules so the crop is taken from the bottom, not the top. Shared rules = global to all listings, which is correct: top-anchoring shows the full banner where there is one and harms none where there is not.
- [x] **Scope check - Island Girl (no banner, `yachts/offshore-48-island-girl.html`, same classes):** hero islandgirl-hero.jpg (1600x895) was already cover-cropping (over 70vh); top-anchor just shifts the trim to the bottom water - verified visually the full boat + sky still show, nothing important clipped. Gallery (portrait 01 + 4:3 02-06) tiles also read fine top-anchored. No regression.
- [x] **Verified visually (headless Chrome 1280 wide):** Fortunato hero now shows the full "PRICE REDUCTION" banner; Fringe Benefits gallery first tile shows the full banner; Island Girl hero + gallery confirmed still correct. Commit `9b82976`, sync-push clean (`ff969ac..9b82976`).

### PRICE REDUCTION band -> solid opaque black (Riv 545 + Southern Wind) - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (origin had moved: admin-tool commits + featured-card Island Girl PDF fix, all already in local main 6bdda64; clean rebase, nothing behind). Clark liked the lighter lettering + the match but wanted ONE change: the band behind PRICE REDUCTION should be SOLID 100% black, identical on both boats - not the dark-navy semi-transparent look.
- [x] **Only the band fill changed:** `BAND_RGBA` in `bake-price-banner.py` went from `(10,14,22,158)` (dark navy-black ~62% opacity, see-through) to **`(0,0,0,255)`** (pure black, full opacity, no photo through). Everything else kept exactly as approved: red Open Sans SemiBold all-caps, same 11.5% band height/position/proportion, matched between the two boats. Re-baked from the CLEAN recovered masters (`c0c2ca2^` for Riv, `ece0dbc:southern-wind.original.jpg` for SW) - NOT stacked over the current banners.
- [x] **4 files re-baked:** `images/yachts/featured/riviera545suv.jpg`, `articles/newsletters/images/riviera545suv.jpg`, `images/yachts/featured/southern-wind.jpg`, `articles/newsletters/images/southern-wind.jpg`. Each boat's two copies `cmp`-verified byte-identical.
- [x] **Verified visually** - opened both re-baked featured images: band is now solid opaque black (zero photo showing through), red text reads cleanly on pure black, and the two still match as a set (band height/position/proportion + font + red + centering).
- [x] **Script header/comment updated** to describe the solid-black band so the bake stays repeatable. Commit `481bb05`, sync-push clean (`6bdda64..481bb05`).

### PRICE REDUCTION banners re-baked (Riv 545 + Southern Wind) - less bold + matched - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (clean, in sync w/ origin/main 7ed2696). Clark: the baked PRICE REDUCTION banners on the Riv 545 and Southern Wind 72 were too heavy (Impact + thick black stroke). Wanted lighter lettering and the two banners to MATCH, using the Riv 545 band as the reference.
- [x] **Clean masters recovered from git history (NOT painted over old banners):** Riv 545 = blob at `c0c2ca2^` (`git show c0c2ca2^:images/yachts/featured/riviera545suv.jpg`, 1600x1176). Southern Wind = the deleted pre-overlay master `southern-wind.original.jpg` recovered from `ece0dbc` (`git show ece0dbc:images/yachts/featured/southern-wind.original.jpg`, 4800x3200). Both verified clean (no banner) by viewing before bake.
- [x] **New matched banner spec (Riv 545 = reference):** dropped Impact for **Open Sans SemiBold** (the brand face), bundled in-repo at `images/yachts/featured/fonts/OpenSans-SemiBold.ttf` so the bake is repeatable. Band = top 11.5% of image height, dark navy-black `(10,14,22)` at ~62% opacity semi-transparent. Text RED `(214,30,30)`, all-caps, centered, ~52% of band height, hairline `(40,0,0)` stroke at ~1.8% of font size (subtle, not the old slab). SW downscaled to 1600px wide to match the live delivery convention so both render at the same scale.
- [x] **Reusable script** `images/yachts/featured/bake-price-banner.py` (Pillow) with the clean-master sources documented in the header comment. Writes each boat to BOTH the featured path and the newsletter copy, byte-identical (verified `cmp` for both boats).
- [x] **4 files written:** `images/yachts/featured/riviera545suv.jpg`, `articles/newsletters/images/riviera545suv.jpg`, `images/yachts/featured/southern-wind.jpg`, `articles/newsletters/images/southern-wind.jpg`.
- [x] **Verified visually** - opened both re-baked images: (a) text clearly lighter than the old Impact, (b) the two banners match (band height/opacity/position, font, red, centered), (c) no ghosting from a prior banner (baked from clean masters), (d) boat hulls not newly covered (text sits in the top band over sky/sail-top). Commit + sync-push (hash below).

### Island Girl - Haley-branded spec sheet PDF built + linked (replaces removed Denison brochure) - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (in sync w/ origin/main 91737d2). Clark wanted a "Download Spec Sheet (PDF)" on the Island Girl listing, but the broker brochure was a no (Denison disclaimer, already removed). Built a clean OWYG-branded sheet from our own data instead.
- [x] **Source HTML** (`documents/yachts/island-girl/src/1997-Offshore-48-Island-Girl-Spec-Sheet.html`) - Letter-size print doc, kept in the repo so the PDF can be regenerated. On-brand: navy #0a1628 masthead w/ "Haley Yachts" wordmark + cyan #21cbea accent bar, "Part of One Water Yacht Group" tagline, Clark NAP block (2401 PGA Blvd Suite 164 / 561-817-1547 / clark@haleyyachts.com matching the footer). Hero image at top, title "1997 Offshore Yachts Sedan - Island Girl", Asking $330,000, Patrick's About narrative (Offshore-pedigree paragraphs, NO Offshore-vs-Denison text), and the full spec set in two columns grouped Vessel / Power / Accommodations / Galley / Mechanical-Electrical / Electronics / Tanks / Deck-Exterior. Disclaimer in HALEY YACHTS' name (good-faith, subject to prior sale/price change/withdrawal) - NOT Denison.
- [x] **Rendered to PDF** via headless Google Chrome (`--headless --disable-gpu --no-pdf-header-footer --print-to-pdf`, file:// abs path so the hero JPEG resolves). Output `documents/yachts/island-girl/1997-Offshore-48-Island-Girl-Spec-Sheet.pdf` = 350 KB, 1 page (verified via `file`: "PDF document, version 1.4, 1 pages"). Visually verified by rasterizing the actual PDF (qlmanage) and reading it back: masthead, hero, title+price, two-column specs, disclaimer, navy footer all render clean and on-brand.
- [x] **Linked from the listing** (`yachts/offshore-48-island-girl.html`): mirrored fortunato's two-button `.listing-actions` block - added `Download Spec Sheet (PDF)` (`.btn`, target=_blank rel=noopener) next to `Schedule a Showing` (now `.btn-outline`). Also added a direct download button inside the Key Specs aside (the "spec sheet card") and rewrote the old "contact for the complete spec sheet" note to point at the sheet. Both hrefs = `../documents/yachts/island-girl/1997-Offshore-48-Island-Girl-Spec-Sheet.pdf`, confirmed to resolve to the committed file.
- [x] **Serving:** no `documents/.htaccess`; root .htaccess only blocks dotfiles + `.md`, so `documents/*.pdf` serve publicly (same as fortunato's PDF). Verified.
- [x] **Checks:** PDF has 0 "Denison"; page + source HTML each have 0 em dash / 0 OWYS / 0 Denison. PDF tracked by git. Commit + sync-push (hash below).

### Featured card PRICE REDUCTION banner half-cut on Riviera 545 - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (clean, in sync w/ origin/main 9d00751). Clark: the baked "PRICE REDUCTION" banner across the top of the Riviera 545 "Fringe Benefits" featured card image was ~50% clipped.
- [x] **Root cause confirmed:** featured cards render from `js/featured-yachts.js` into the fixed-height `.card-img` box (`css/styles.css` line 327, `height: 220px`). The renderer set the inline image style `background-size: cover; background-position: center`. The 545 photo (1600x1176, aspect 1.36) cover-fills the wider card box (~370x220, aspect 1.68): displayed height ~272px, so ~26px is trimmed top AND bottom by the centered crop. The baked banner band (~11.5% = ~31px displayed) sits at the very top, so ~26px of it was clipped = the "half cut" Clark saw. Same issue on Southern Wind (also a baked top banner).
- [x] **Fix (one line, `js/featured-yachts.js`):** changed the inline `imgStyle` from `background-position: center` to `background-position: center top`. Single source renders BOTH index.html and buy.html featured sections (same `#featured-yachts-grid` + same script), so one edit covers both. Chose this over raising the box height (would reskew every card site-wide; cropping is per-card anyway) and over a per-card override (a top-anchor helps every baked-banner card and harms none).
- [x] **Verified visually** - headless Chrome screenshot at the real 370x220 card size: Riviera 545 full banner now shows, boat fully in frame; Southern Wind full banner + "Fortunato" name now show (it had the same bug); Island Girl (no banner) keeps the full boat, trims only bottom water foreground - actually a better crop than centered. Other cards (465 SUV, Sunseeker, Absolute) are plain profile shots, top-anchor just trims bottom water = harmless.
- [x] Did NOT touch the Kuula 360 badge (still bottom-right per the earlier collision fix). Commit `9d3f55c`, sync-push clean (`9d00751..9d3f55c main -> main`). Live appears after Clark's cPanel pull.

### Island Girl (1997 Offshore 48) listing page built + wired in - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (tree clean, in sync with origin/main 01712a6). Built the live detail page for the 1997 Offshore Yachts Sedan "Island Girl" off Patrick's approved copy + the broker brochure specs.
- [x] **New page `yachts/offshore-48-island-girl.html`** - cloned fortunato.html structure exactly (header, listing-hero, listing-intro, narrative + Key Specs `<dl>` aside, gallery, inquiry form + submit script, footer, Product JSON-LD). Title/meta/OG/Twitter/positioning/H1 ("Island <strong>Girl</strong>")/two About paragraphs from `docs/drafts/offshore-48-island-girl-listing-copy.md`, PRICE-SET variants, $330,000 throughout. Form `_subject` + mailto = "Island Girl - 1997 Offshore 48". Back link -> ../buy.html#featured.
- [x] **Key Specs** built natively from the brochure (Year/Builder/Model Sedan/LOA 48 ft/Beam 15 ft 6 in/Power Twin Caterpillar 3126-TA 420 HP/Engine hours ~2,000 (2006)/Staterooms 2/Heads 2/Type/Asking $330,000) + the "contact for the complete spec sheet" note. JSON-LD Product: brand Offshore Yachts, model Sedan, 1997, category Motor Yacht, price 330000 USD InStock, hero image, additionalProperty LOA/Beam/Power/Type, seller @id #business. Both JSON-LD blocks validate.
- [x] **Images** copied from staging into `images/yachts/island-girl/` (hero `islandgirl-hero.jpg`) + `images/yachts/island-girl/gallery/gallery-01..15.jpg` - NOT re-compressed (already web-sized, ~3.6MB total). Hero alt "1997 Offshore 48 Island Girl on the water". All 16 paths verified to resolve.
- [x] **Gallery** = responsive CSS-grid of all 15 images using the existing `.listing-gallery-grid` (auto-fit minmax 280px = 3-up desktop / 2 tablet / 1 mobile, 16px gap, rounded corners, object-fit cover 3:2, lazy-loaded). Descriptive per-image alts in the exterior->flybridge->cockpit->salon->helm->galley->master->guest->head->engine order. No lightbox, no "coming soon" note. NO 360 section (no Kuula tour). NO PDF button (broker brochure carries a Denison disclaimer) - replaced with a "Schedule a Showing" CTA only.
- [x] **DENISON / brochure:** removed both copies of the Denison-disclaimer brochure from the repo via `git rm` (`documents/yachts/featured/1997-Offshore_Yachts-Sedan.pdf` + `...(1).pdf`) and cleared its reference from the featured card so it is not publicly linkable. Zero "Denison" / "OWYS" / em-dash anywhere on the page.
- [x] **Featured Yachts wire-up:** Island Girl was already card #3 in `js/featured-yachts.js` (image `islandgirl-2.jpg`). Set `page: "yachts/offshore-48-island-girl.html"`, cleared the Denison `pdf`, and baked the price into the name string ("1997 Offshore 48 - ISLAND GIRL $330,000") per the schema. Card image + title + primary action now route to the page (renderer shows "View Listing"). FLAG: featured-yachts.js is also writable by the browser admin tool (Featured Image Maintenance); code-level link is correct now, but if Clark re-saves via the admin tool he should reconfirm the page link + price stick.
- [x] **sitemap.xml** - added the new URL (priority 0.9, lastmod 2026-06-16).
- [x] **sync-footer.sh** - added the new page to the hard-coded list; ran `sync-footer.sh` (0 of 29 changed = the page's footer is byte-identical to the partial, schema matches every other page).
- [x] **Newsletter** (`email-templates/issues/logbook-2026-06.html`) - Island Girl secondary card: CTA changed mailto -> "View Listing" linking the live page with the logbook-2026-06 UTM (matching the Fortunato card pattern), spec line now "$330,000", removed the stale FLAG comment. islandgirl-2.jpg image left as-is.
- [x] **Checks:** structural tag balance OK (section 4/4), all image paths resolve, em dash = 0, OWYS = 0, Denison = 0. Commit + sync-push (hash below).

### Logbook June - three edits (logo bar removal, masthead panel re-bake, 545 price banner) - SHIPPED 2026-06-16 (Clark via William)
`git fetch` first (tree clean, in sync with origin/main 9fcf2eb). Three edits to the June Logbook.
- [x] **ITEM 3 - removed the top logo bar `<tr>`** (white wordmark on the `#11203a` navy panel that sat ABOVE the masthead composite) from BOTH `email-templates/issues/logbook-2026-06.html` AND `email-templates/logbook.html`. Clark found it redundant with the Haley logo baked into the masthead. Masthead composite `<tr>` directly below left intact. Tag balance re-verified: tr open=close (37 issue / 36 template), table open=close (16/16).
- [x] **ITEM 4 - re-baked masthead with a ~50%-opacity panel behind the Haley logo.** Edited `bake-masthead.py`: after sizing the Haley wordmark, draw a brand-navy `(8,22,40,128)` rounded rectangle (radius 14, ~24x18 px padding) on its own RGBA layer, alpha-composite onto the canvas, then paste the logo on top. Re-baked to `images/email/logbook-masthead.jpg` at the SAME 1200x720 / filename / path (119,580 bytes). The white wordmark now pops cleanly against the bright sky. Original recoverable from git (c3fe616) + /tmp backup. Source assets all present; passed explicit absolute --photo/--logo/--cobrand/--out (script DEFAULTS use a `haleyyachts-website/` prefix that does not match this repo's root, which IS the GitHub/ clone). NOTE: renders live only after Clark's cPanel pull.
- [x] **ITEM 2 - PRICE REDUCED banner on the 545 card (issue file only).** Added a new full-width red bar `<tr>` as the FIRST row of the hero-card table, above the image: `bgcolor="#d61e1e"` + inline background-color (MSO-safe, no rgba/opacity), white bold uppercase letter-spaced "Price Reduced - Now $1,295,000". Belt-and-suspenders so the drop reads even before the cPanel pull bakes the image banner in.
- [x] **Checks passed:** em dash (U+2014) = 0 in both files; "OWYS" = 0 in both; all real issue UTM links read logbook-2026-06 (the one `logbook-` hit is the `YYYY-MM` instructional comment on line 21, not a link).
- [x] Commit + sync-push (hash below). Left Patrick's pre-existing uncommitted `docs/tasks/patrick.md` + `docs/drafts/offshore-48-island-girl-listing-copy.md` UNTOUCHED per single-writer rule (staged only my files).

### Riviera 545 SUV social cards re-rendered: new price + PRICE REDUCED - SHIPPED 2026-06-15 (Clark via William; Pillow now installed v11.3.0)
`git fetch` first (tree clean, up to date with origin). Closes the FOLLOW-UP flag from the price-reduction task: the two `social-media/cta-cards/*.py` render scripts still hardcoded $1,495,000. Pillow is installed now, so I rendered them.
- [x] **Scripts updated:** `render-cta-cards.py` and `render-360-cards.py`. Repointed the hardcoded `/Users/jameschaley/...` OUT_DIR/LOGO_PATH/PHOTO_DIR to a `_REPO_ROOT` derived from `__file__` (two levels up) so the render is machine-independent (this Mac is `/Users/remoteshop`). Added a `draw_price_reduced_badge` helper to each (centered red `#e44040` rounded pill, white bold uppercase letter-spaced "PRICE REDUCED", Arial Bold - same font family the scripts already use).
- [x] **Price 1,495,000 -> 1,295,000** on every Fringe Benefits line: CTA card subtitle + both 360 sub_lines (1920 and 1350). Fortunato ($395,000) left untouched (different boat); its two PNGs restored after the shared run re-encoded them byte-wise with identical content.
- [x] **Badge placed per layout:** CTA card = under the price line; title card (no price shown) = between the accent line and the hull name; 360 cards = under the sub/price line via a new `price_reduced=True` arg on `render_card` (Fortunato calls omit it, no badge). The 360 1920 photo also carries the pre-existing baked-in "PRICE REDUCTION" top banner from `riviera545suv.jpg` (out of scope, left as-is); the 1350 crops it out, so my badge is the only reduction cue there.
- [x] **Re-rendered + visually verified** all four Fringe Benefits outputs show $1,295,000 + PRICE REDUCED: `fringe-benefits-cta-1080x1920.png`, `fringe-benefits-title-1080x1920.png`, `fringe-benefits-360-1080x1920.png`, `fringe-benefits-360-1080x1350.png`. Same filenames, nothing referencing them breaks. These are export assets (IG/ESP), not served by the site, so no cPanel pull needed.
- [x] **Font note:** scripts already substitute Arial / Arial Bold for the brand Open Sans (PIL renders it cleanly); kept that. No missing-asset fallbacks needed - logo, photo, and fonts all present.
- [x] Commit(s) + sync-push (see commit hash below).

### Logbook June 2026 issue + masthead logo bar - SHIPPED 2026-06-15 (William handed task)
`git fetch` first (tree clean, up to date with origin). Two-part Logbook job.
- [x] **Masthead logo bar (HTML, no re-bake of the JPG).** Added a new `<tr>` ABOVE the masthead composite in BOTH `email-templates/logbook.html` (campaign stays logbook-2026-05) and the new issue `email-templates/issues/logbook-2026-06.html` (logbook-2026-06). White wordmark `haleyyachtslogo-footer.png` (hosted absolute URL), 220x39 explicit w/h, `display:block`, alt="Haley Yachts", wrapped in an `<a>` to haleyyachts.com with the issue's UTM (`utm_content=site-link`). Low-opacity-glow simulated email-safe with a marginally lighter SOLID navy panel - inner table cell `bgcolor="#11203a"` + inline `background-color:#11203a`, `border-radius:8px`, padding `16px 28px`; outer cell padding `16px 0` on the `#0a1628` masthead bg. No rgba/opacity (clients strip it). Did NOT run bake-masthead.py.
- [x] **Built June issue** from a copy of the template: rolled all `utm_campaign=logbook-2026-05` -> `logbook-2026-06` (whole file, 0 left of -05, 16 of -06), month banner -> "June 2026", Bahamas preheader, new "A Note from Clark" (OWYG Rendezvous July 16-19, Freeport), kept Fair winds/Clark sign-off + all structure/MSO buttons/footer/signature/Work With Clark/OWYG blocks.
- [x] **Featured listings (3):** HERO = 2020 Riviera 545 SUV "Fringe Benefits" (riviera545suv.jpg, price-badge already baked in, View Listing -> yachts/fringe-benefits.html). Secondary L = 1991 Southern Wind 72 "Fortunato" (southern-wind.jpg -> yachts/fortunato.html). Secondary R = 1997 Offshore 48 "Island Girl" (islandgirl-2.jpg). Browse All Listings -> buy.html with UTM.
- [x] **FLAG - Island Girl:** no live listing page or confirmed price yet. Link is `mailto:clark@haleyyachts.com` ("Inquire ->"), spec reads "Price on request", inline HTML comment placed on the card. Clark to decide price + whether I build a page.
- [x] **Articles (4) in From the Dock 2x2:** (1) OWYG Bahamas Rendezvous announce -> industry-news article. (2) Provisioning/Crossing - NOT PUBLISHED, href="#" + "LINK PENDING:" HTML comment to swap in the live URL + re-tag UTM once Patrick's draft is published. (3) Riviera 4300 Sports Express -> boat-reviews article. (4) How to Plan Your First Bahamas Cruise -> how-to article. All live links UTM-tagged read-article.
- [x] **Recent Sales (4):** intro line added under the eyebrow, closer line added below the cards; cards = 2005 Manta 42 Mark III, 2027 Prestige F5.7, 2010 Sunseeker Predator 84, 2027 Riviera 6200 Sport Yacht. All marked Sold, hyphens only.
- [x] **Checks passed:** em dash char (U+2014) count = 0 in both files; "OWYS" misspelling = 0 in both; all issue UTM tags read logbook-2026-06.
- [x] Single commit 288de5d covering both files. sync-push clean, fast-forward push b2a236f..288de5d, no rebase needed. NOTE: tree had two PRE-EXISTING `drafts/*.json` deletions (not my work, appeared after handoff) - I staged ONLY my two files, stashed the unrelated deletions so sync-push had a clean tree, then popped the stash to leave that state exactly as found.

### 360 Tour badge overlapping baked-in PRICE REDUCTION banner - SHIPPED 2026-06-15 (Clark spotted on Southern Wind via William)
`git fetch` first (tree clean, up to date with origin). Root cause: the featured-card 360 badge (`.fy-kuula-badge`, injected by `js/featured-yachts.js`) was anchored `top: 12px; right: 12px` - the exact spot where the PRICE REDUCTION bar is baked across the TOP of the featured image, so the badge sat on top of the banner text. Collision is featured-cards-only (homepage + buy.html); the LISTING pages (`fortunato.html`, `fringe-benefits.html`) use a plain `listing-hero-img` with no image overlay and a separate 360 section lower down, so no collision there.
- [x] **Fix:** moved `.fy-kuula-badge` from `top: 12px` to `bottom: 12px` (kept `right: 12px`) in the injected CSS in `js/featured-yachts.js`. Bottom-right keeps it clear of the top banner. General fix - applies to ANY featured card that has a 360 tour, not a per-boat hack.
- [x] **Affected boats:** both currently-badged price-reduction cards - Southern Wind 72 "Fortunato" (`southern-wind.jpg`, `yachts/fortunato.html`, kuula 7MFLp) and Riviera 545 SUV "Fringe Benefits" (`riviera545suv.jpg`, `yachts/fringe-benefits.html`, kuula 7MBqZ). Confirmed Southern Wind 72 = Fortunato = `yachts/fortunato.html`.
- [x] **Verification:** code-verified, not visually rendered in a browser this session. `.card-img-wrap` is `position: relative` and wraps the fixed 220px cover-image box, so a `bottom: 12px` badge lands inside the image away from the top banner. No competing badge rule in `styles.css`.
- [x] Commit 0354f57 (badge CSS), plus 2f42351 (untracked 3 stray tracked `.DS_Store` files - already gitignored). sync-push clean (c53b311..2f42351).

### Fringe Benefits price reduction + Price Reduction badge - SHIPPED 2026-06-15 (William handed task; $200K reduction, verified current $1,495,000 -> new $1,295,000)
William pre-fetched/rebased the working tree; did not re-rebase before editing.
- [x] **Price updated everywhere on the listing page** `yachts/fringe-benefits.html`: title, meta description, og:title, product:price:amount (1295000), twitter:title, JSON-LD offer price (1295000), on-page `.listing-price`, and the Asking spec `<dd>`. All 8 spots now read $1,295,000.
- [x] **featured-yachts.js was ALREADY at $1,295,000** - the browser Featured Yacht editor had already committed the new price directly to GitHub (price is baked into the `name` string; there is no separate price field on the object). Left as-is, confirmed consistent. buy.html has no price for this boat (image refs only), nothing to change.
- [x] **Price Reduction treatment (pure CSS, no image editing).** Toolchain has no ImageMagick/Pillow/ffmpeg, so did NOT touch the JPG. Added markup in `fringe-benefits.html` (a `.price-reduction-tag` span beside the price, a `.price-reduction-ribbon` div in the hero) and CSS in `css/styles.css`: on-brand cyan tag (#21cbea on navy text) next to the price, and a red (#c0392b) rotated corner ribbon on the listing hero with `.listing-hero { overflow:hidden }` clipping it to the image. Listing page only.
- [x] **Featured-card ribbon NOT added.** The featured cards render generically from featured-yachts.js (shared template, no per-card flag), so a clean card ribbon would need either a new schema field or boat-specific markup. RECOMMENDATION: if Clark wants the badge on the buy.html featured card too, add an optional `badge` field to the featured-yachts schema + render it in the card template - small, clean change, flagging rather than hacking it in.
- [x] **Re-grep:** remaining $1,495,000 / 1495000 hits are the four William-flagged do-not-touch files (logbook.html template + its .txt twin, the two email-templates/issues sends, the May 2026 published newsletter) PLUS Patrick's two social-card render scripts (`social-media/cta-cards/render-cta-cards.py`, `render-360-cards.py`) and a history line in patrick.md. FLAGGED for Patrick: those Python render scripts still hardcode $1,495,000 for this boat - re-rendering the Fringe Benefits CTA/360 cards is Patrick's job (he has Pillow), out of website scope, did not touch.
- [x] Two commits: ae680cc (price + badge markup), 1552017 (badge CSS). sync-push clean (0851506..1552017).
- [x] **FOLLOW-UP 2026-06-15: baked the badge INTO the image (Pillow now installed, v11.3.0).** Clark wanted PRICE REDUCTION on the Riviera 545 SUV image itself so it travels with the file (newsletter, social, featured card, listing hero), not just CSS on the listing page. Matched the existing Southern Wind overlay style: full-width dark (rgba 15,15,20,~200) banner across the TOP, bold red (#d61e1e) all-caps "PRICE REDUCTION" in Impact with a black stroke, centered, band ~11.5% of image height. Badged BOTH referenced copies (identical MD5 before and after): `images/yachts/featured/riviera545suv.jpg` and `articles/newsletters/images/riviera545suv.jpg`. Same filenames, clean originals recoverable from git, no orphan backups. ~380KB each.
- [x] **CSS reconciliation:** removed the `.price-reduction-ribbon` CSS rule + its hero `<div>` and the duplicate `.listing-hero { overflow:hidden }` that only existed to clip it - the baked banner would otherwise double up on the listing hero. KEPT the small cyan `.price-reduction-tag` beside the price (sits in the text section below the hero, reads well, not redundant). Updated hero alt text to note the reduction.
- [x] Commits c0c2ca2 (badged images + removed tracked .DS_Store) and 84ca335 (CSS/HTML ribbon removal).

### Hero video re-encode - BLOCKED - needs ffmpeg installed (Clark approved task 2026-06-15)
Goal: re-encode the homepage hero `images/video/home-header-video.mp4` (currently 14,634,665 bytes / ~14.6 MB) down to a web-appropriate ~3 MB, H.264/MP4, KEEPING the exact same filename + path so nothing breaks (index.html line 68 `<source src="images/video/home-header-video.mp4" type="video/mp4" media="(min-width: 768px)">`), keeping the existing poster. Add a WebM secondary source only if cheap to produce.
- **BLOCKED: no video encoder of any kind is installed on this machine, and there is no Homebrew to add one.** Verified directly this session: `which ffmpeg ffprobe HandBrakeCLI avconv` all not found; nothing in `/opt/homebrew/bin`, `/usr/local/bin`, `/opt/local/bin`; no `/Applications/HandBrake.app` or VLC; no gstreamer; no python `imageio_ffmpeg`/`moviepy`; `mdfind` finds no ffmpeg binary anywhere. The only media tools present are `/usr/bin/sips` (images only) and `/usr/bin/afconvert` (audio only) - neither can transcode an MP4. `which brew` = not found, no brew at either prefix.
- **Did NOT fake it.** No smaller-but-fake file was produced. The 14.6 MB file is untouched.
- **What Clark needs to run to unblock** (one of):
  - Install Homebrew, then ffmpeg: `/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"` then `brew install ffmpeg`.
  - OR drop a static ffmpeg build on PATH (e.g. from https://evermeet.cx/ffmpeg/ for Apple Silicon/Intel macOS), unzip and `chmod +x`, place in `/usr/local/bin`.
  - OR install the HandBrake desktop app and re-encode in the GUI to the target below.
- **Target spec** once an encoder exists: H.264 MP4, ~3 MB, same path `images/video/home-header-video.mp4`. Suggested ffmpeg: `ffmpeg -i in.mp4 -c:v libx264 -crf 28 -preset slow -vf "scale=1280:-2" -movflags +faststart -an out.mp4` (drop audio with `-an` since it is a background hero; tune `-crf` 26-30 to land near 3 MB). Optional WebM: `ffmpeg -i in.mp4 -c:v libvpx-vp9 -crf 34 -b:v 0 -vf "scale=1280:-2" -an out.webm`, add as a second `<source type="video/webm">` ABOVE the mp4 source. Poster stays as-is. Re-confirm index.html reference + poster after, then commit + sync-push.

### Email mobile overflow fix - masthead/footer logos - SHIPPED 2026-06-11 (Clark reported co-broke email too wide on iPhone)
`git fetch` first (pulled in Patrick's GBP commit c139699 on rebase; sync-push handled the divergence). Root cause: viewport meta WAS present and the container/hero/signature already went fluid, but the masthead dual-logo lockup (Haley 201px + 24px spacer + One Water 177px = 402px fixed-width row) had NO mobile fallback and forced horizontal overflow on iPhone portrait (375-430px), pushing the whole email wider than the screen. Footer logos (240px / 180px) were a secondary risk.
- [x] **Added media-query rules** (`@media max-width:600px`): `.logo-cell` goes `display:block; width:100%` so the two marks stack and center; `.logo-spacer` hidden; `.logo-img` capped at `max-width:70%; height:auto`; `.footer-img` capped at `max-width:70%; height:auto`. Masthead pad trimmed 24px -> 18px.
- [x] **Tagged the markup** with the matching `logo-lockup`/`logo-cell`/`logo-spacer`/`logo-img`/`footer-img` classes. Inline px widths kept intact for Outlook/desktop; mobile rules only override via the media query, so desktop rendering is unchanged.
- [x] **Applied to BOTH** the source template `email-templates/general.html` (every future email inherits the fix) AND the saved issue file `email-templates/issues/riviera-545-suv-broker-cobroke.html` (Clark's current send).
- [x] **Verified**: viewport meta present, container has `width:100%` fallback under `max-width:600px`, media query targets correct width, hero already `width:100%`, signature already stacks via `.stack`. Audited every 3+ digit width - only the masthead lockup lacked a fluid fallback (VML 600px widths are Outlook-desktop only). One commit (dd26fe8), sync-push to GitHub. Clark re-imports/previews the updated file when building the send; nothing to deploy for the email.

### Business hours added to schema + contact page - SHIPPED 2026-06-11 (Clark supplied hours via William)
Closes the long-standing "no business hours supplied" flag from the LocalBusiness JSON-LD work. `git fetch` first to clear William's just-pushed GBP commit (931cee3) before reasoning about remote state.
- [x] **JSON-LD `openingHoursSpecification`** added to the single-source business schema in `partials/footer.html`, after `areaServed`. One `OpeningHoursSpecification` covering Mon-Fri: `dayOfWeek` = [Monday, Tuesday, Wednesday, Thursday, Friday], `opens` "09:00", `closes` "17:00".
- [x] **Saturday/Sunday deliberately NOT in JSON-LD.** They are "by appointment" and schema.org has no clean by-appointment value. Did NOT invent fake weekend open/close times - only the Mon-Fri spec is structured.
- [x] **Human-readable hours line** added to `contact.html`, in the centered `.contact-info` block right under the street address: "Mon-Fri 9:00am-5:00pm / Sat-Sun by appointment". This was the natural existing spot (NAP block); no awkward forcing needed.
- [x] **Propagated** via `sync-footer.sh`: 28 of 28 public pages updated, `--check` returns CHECK OK. JSON-LD parses valid (verified Mon-Fri spec present on the footer business schema across all synced pages). Zero broken output. Admin/, email-templates/, drafts/, cta-card template excluded by design.

### Orphan / duplicate image cleanup - SHIPPED 2026-06-11 (Clark approved: "remove the duplicate or unused items, photos")
`git fetch` first (local even with origin/main, no admin-tool divergence). Repo-wide raster audit: for every jpg/jpeg/png/gif/webp, checked whether the basename AND exact path is referenced anywhere in served files (html/css/js, articles-data.js, featured-yachts.js, manifest.json, sitemap.xml, robots.txt). One logical commit, `git rm` + sync-push to GitHub only (Clark runs the cPanel pull).
- [x] **DELETED 5 unreferenced orphans/duplicates (20,790,369 bytes, ~19.8 MB):**
  - `articles/travel/images/clark-haley-headshot.jpg` (10.25 MB) - unreferenced 5504x8256 duplicate; travel articles use the `images/people/` headshot. Different md5 from the people copy (separate hi-res export) but still a dead orphan.
  - `images/yachts/featured/southern-wind.original.jpg` (7.93 MB) - pre-overlay master backup, unreferenced; recoverable from git history.
  - `images/yachts/stock/yacht-overhead.jpeg` (637 KB) - unreferenced stock orphan.
  - `articles/how-to/images/clark-lean-crop-with-background.jpg` (591 KB) - unreferenced. The SQUARE variant `clark-square-lean-crop-with-background.jpg` IS used (how-to-choose-a-yacht-broker hero) and was KEPT.
  - `360/kuula-card copy.png` (460 KB) - Finder duplicate (" copy" suffix), unreferenced; the 360/ Kuula cards are served via Kuula iframe embeds, not this file.
- [x] **Verification:** post-delete grep for all 5 basenames/paths across html/css/js/json/xml/webmanifest/txt = ZERO hits. sitemap.xml + data files had no entries to clean (all 5 were unreferenced). 0 broken image references introduced.
- **KEPT, not deleted (intentional off-site / future-use assets, per Clark's keep-list + judgment):**
  - `social-media/cta-cards/*.png` (6 files, ~3.9 MB) - Instagram export assets, not served by the site.
  - `images/email/**` incl. `hero/*` (the 1-1.2 MB ESP newsletter heroes), `nordhavn80.jpeg` (referenced by `email-templates/bake-masthead.py`), banners - ESP/newsletter use.
  - `360/` Kuula cards - covered above; kept the canonical card, removed only the " copy" dupe.
  - `images/people/BBP_2820-2.jpg` (124 KB) - staged studio headshot, plausible future Article Manager insert.
  - `images/brand/favicon-16/32/48/180/192/512.png` - the multi-size favicon export set. Site currently wires only `favicon.ico` + `favicon.svg`, so the PNG set reads "unreferenced," but it is a coherent generated favicon/PWA set, not a duplicate. Left it. FLAG for Clark: if we never want PNG favicons we could drop them, but they are tiny and harmless.
  - `images/graphics/n96.jpg`, `images/yachts/featured/2022-sunseeker-manhattan-68.jpg`, `images/yachts/stock/yacht-on-plane.jpeg` - currently unreferenced but look like legit yacht/brand stock kept for future listing/marketing use. Low confidence they are dead; KEPT and flagging rather than deleting.
- **Duplicate brand-asset DIRECTORY - RESOLVED 2026-06-11 (Clark: "get rid of the duplicate brand art").** Re-verified byte-identical first: all 6 `HALEY YACHTS` source files (.ai/.eps/.pdf/.jpg/.png) md5-match pairwise between `logo-haley-yachts-logo/` and `images/brand/favicon_logo/`, and `diff -r` exits 0. Repo-wide ref check (HTML/CSS/JS/JSON/XML/manifest/txt/htaccess) found ZERO served references to either path; only internal docs mention it, all pointing at the keeper. Removed `logo-haley-yachts-logo/` via `git rm -r`; KEPT `images/brand/favicon_logo/` because it is the documented canonical location (cited in `images/brand/INDEX.md` and `docs/logo-rebuild-brief.md`). Freed 11,755,281 bytes (~11.2 MB) - my earlier "~22MB each" was an overcount; real per-dir size is ~11.7MB (the two .eps are ~5.5MB each, not the bulk I'd assumed). Post-delete grep = 0 broken refs. Commit 8c1847d, sync-push clean. Recoverable from git history; goes live on Clark's next cPanel pull.

### Technical SEO tightening batch - SHIPPED 2026-06-10 (authorized by Clark, run via William)
Five logical commits, each pushed via `scripts/sync-push.sh` (GitHub only; Clark runs the cPanel pull). NOTE on workflow: a concurrent Patrick session had an uncommitted edit to `docs/tasks/patrick.md` sitting in the working tree the whole batch. Per the single-writer rule I never touched/committed it - I stashed it around each sync-push and popped it back after, so every one of my commits stayed isolated to my own files and Patrick's parked work was preserved. No rebase conflicts; all five pushes clean.
- [x] **sitemap.xml + robots.txt** (root). Commit `e2aac7f`. Sitemap: 9 core pages + 3 yacht listings + 15 published articles (from `articles/articles-data.js`), absolute `https://haleyyachts.com/` URLs, lastmod dates. The orphan `articles/travel/2026-05-05-bahamas-cruising-permit-changes-again.html` (self-canonical duplicate of the `-april-2026` file, NOT in articles-data.js) was deliberately EXCLUDED from the sitemap - see flag below. robots.txt: allow crawl, `Sitemap:` line, Disallow `/admin/`, `/docs/`, `/drafts/`.
- [x] **Sitewide LocalBusiness JSON-LD** via the footer partial. Commit `974c896`. Added `["ProfessionalService","LocalBusiness"]` block (NAP, geo for Jupiter FL 26.9342/-80.0942, sameAs the two real socials, parentOrganization "One Water Yacht Group", logo, founder Clark Haley) to `partials/footer.html` and propagated to all 29 public pages via `sync-footer.sh`. Also fixed a sync-footer coverage gap: added the 4 newer pages that were missing from the PAGES list (3 June-06 articles + the Riviera 4300 listing). `streetAddress` and `openingHours` OMITTED on purpose (not supplied) - see flags.
- [x] **Canonical + OG + Twitter on the 7 money pages** (index, buy, sell, services, about, contact, valuation). Commit `c15bd4f`. og:type=website, per-page titles/descriptions, absolute URLs, sensible per-page og:image.
- [x] **Product JSON-LD + canonical + OG/Twitter on the 3 yacht listings.** Commit `77090d1`. schema.org Product (brand/model/year/length/specs/image) + Offer with real asking price for Fortunato ($395k) and Fringe Benefits ($1.495M). Riviera 4300 is a new build with no public price, so its Offer block was omitted, not invented.
- [x] **Cheap CWV wins.** Commit `704f221`. Homepage hero MP4 `<source>` now gated to `(min-width:768px)` so phones render the poster only and never fetch the 14MB file (added `preload="metadata"`). Clark headshot (10.5MB, shown at 280px in the deep inquiry section) set `loading="lazy"` on all 3 listing pages. Alt-text audit: homepage + all 3 listings already had complete non-empty alts; nothing missing.

### Canonical business address reconciled sitewide - SHIPPED 2026-06-11 (Clark confirmed address via William)
Canonical NAP address: **2601 PGA Blvd, Palm Beach Gardens, FL 33410** (no suite supplied). One logical commit, sync-push to GitHub only (Clark runs the cPanel pull).
- [x] **Schema address completed in the footer partial** (`partials/footer.html`, single source of truth). Added `streetAddress "2601 PGA Blvd"` + `postalCode "33410"`, changed `addressLocality` Jupiter -> **Palm Beach Gardens**, and corrected `geo` from the old Jupiter coords (26.9342/-80.0942) to **26.8448 / -80.0752** (2601 PGA Blvd, Palm Beach Gardens). Propagated to all 28 public pages via `sync-footer.sh` (`--check` clean, 28/28). The footer copyright locality line also flipped Jupiter -> Palm Beach Gardens via the partial.
- [x] **Privacy page city fixed.** `privacy.html` "Contact us" block read "2601 PGA Blvd., West Palm Beach, FL 33410" - street number was right, CITY was wrong. Corrected to "2601 PGA Blvd, Palm Beach Gardens, FL 33410" (also dropped the stray period after Blvd to match the schema/displayed string everywhere).
- [x] **NAP reconciled, displayed full address.** Contact (`contact.html`), valuation (`valuation.html`), and all 3 yacht listing broker-contact blocks (`yachts/fringe-benefits.html`, `fortunato.html`, `riviera-4300-sports-express.html`) previously showed only "Jupiter, Florida" - now all show the identical full string `2601 PGA Blvd, Palm Beach Gardens, FL 33410`.
- [x] **Meta-locality reconciled.** Trailing "Jupiter, Florida." locality in `<meta name/og/twitter description>` on index, about, contact, services, valuation flipped to "Palm Beach Gardens, Florida." (5 pages x 3 tags = 15 tags). about.html headshot `alt` locality "Jupiter Florida" -> "Palm Beach Gardens Florida".
- [x] **sync-footer.sh PAGES list cleanup.** Removed the stale `articles/travel/...-changes-again.html` entry (that orphan was git-rm'd 2026-06-11; the leftover list entry was aborting the script mid-run with exit 2). Script now runs clean to all 28 pages.
- Phone (561-817-1547) and business name untouched. One Water Yacht Group / OWYG kept correct.
- **FLAG (left as-is, judgment call):** biographical "based in Jupiter, Florida" prose (about.html body + the article author bios) is where Clark *lives*, not the brokerage NAP, so NOT changed - changing personal residence to a business address would be inaccurate. SEO long-tail keyword tags ("yacht broker Jupiter FL" / "yacht for sale Jupiter FL" in two articles) also left - Jupiter is a legit service-area term; Patrick's call if he wants them swapped. Geo coords are approximate to the 2601 block of PGA Blvd; if Clark has an exact suite/unit, give it and I will tighten the suite + coords.

FLAGS / OPEN ITEMS from this batch (relayed to William for Clark):
- **floridayachts.ai forwarding - RESOLVED / DONE 2026-06-11.** Clark changed it in GoDaddy from the masked forward to a clean Permanent 301 redirect to haleyyachts.com (no masking); verified live (returns 301 to haleyyachts.com, HTTPS handshake fixed). Decision: haleyyachts.com stays primary, floridayachts.ai 301s in; floridayachts.com is taken/unavailable.
- **No street address** supplied for the LocalBusiness JSON-LD, so `streetAddress` is omitted (city/state/country only). NOTE: the privacy page uses "2601 PGA Blvd., West Palm Beach, FL 33410" - if that is the brokerage address Clark wants in structured data, give the word and I will add it (and reconcile Jupiter vs West Palm Beach).
- **No business hours** supplied, so `openingHours` omitted. Provide if wanted.
- **Heavy media re-encoding - IMAGE OPTIMIZATION DONE 2026-06-11 (Clark expanded scope: "get all the images sized properly"). MP4 still open.** Full repo-wide raster pass: 84 jpg/png images resized + recompressed IN PLACE (same filenames/paths, JPEG q80-82, aspect preserved, only downscaling never upscaling) sized to display role - heros/featured/OG 1600px, in-content 1100-1600px, listing gallery/feature strips 1280px, buy.html model cards 1000px. Total ~125 MB saved (148 MB -> 23 MB across the touched files), every changed image now well under ~500KB. Biggest wins: broker2.jpg 14MB->274KB, southern-wind.jpg (newsletter) 8.1MB->382KB, riviera545suv.jpg 6.8MB->404KB (both the featured + newsletter copies), double-wide.jpg 4MB->262KB, the 7 Riviera 6200 boat-review shots ~3MB->~400KB each, plus all 40+ Riviera listing images. `rendezvous1.png` (1.1MB photographic banner, no real transparency) CONVERTED to `rendezvous1.jpg` (139KB); its single `<img>` ref in the OWYG rendezvous article was updated and the old PNG removed. Headshots (clark-haley-headshot.jpg, BBP_2820-2.jpg in images/people/) were already done in the prior pass. Full broken-reference scan after = 0 broken local image refs introduced. Commits `ed8e8ee` (site/listing/featured/lifestyle/email-hero) + `80e653e` (article images + rendezvous png->jpg). FLAGGED, NOT TOUCHED (per Clark - report don't delete): `articles/travel/images/clark-haley-headshot.jpg` (10.5MB, 5504x8256, an unreferenced duplicate of the optimized people headshot) and `images/yachts/featured/southern-wind.original.jpg` (7.9MB, intentional pre-overlay master backup) - both orphans, awaiting Clark's OK before any delete/resize. Also left a couple of already-efficient/unreferenced orphans untouched (yacht-overhead.jpeg, clark-lean-crop-with-background.jpg). Social/email export PNGs under social-media/cta-cards/ + images/email/hero/ are 1-1.2MB but are NOT served by the website (ESP/Instagram assets) - left alone. STILL OPEN: hero MP4 14MB (`images/...`) needs a separate re-encode to ~3MB H.264 + WebM/AV1 variant - OUT OF SCOPE for this image pass; pending.
- **Orphan duplicate article - RESOLVED / DELETED 2026-06-11 (Clark approved delete).** `articles/travel/2026-05-05-bahamas-cruising-permit-changes-again.html` deleted via `git rm`. Confirmed before deleting: keeper is `2026-05-05-bahamas-cruising-permit-changes-april-2026.html` (registered in `articles/articles-data.js` + in `sitemap.xml`); orphan was registered in NEITHER and linked from nowhere. `diff` of the two showed identical body copy - only the title/slug ("Again" vs "April 2026"), the self-referential canonical/og:url/JSON-LD @id, and a trivially reordered inline `<figure>` differed. No dangling references to clean up: articles-data.js and sitemap.xml both pointed only at the keeper, and the orphan was already excluded from the sitemap. The only references to the orphan URL were its own self-canonical/og:url. PLAIN DELETE, no 301: the orphan was never registered, never sitemapped, never internally linked, so it was not discoverable/indexable (its self-canonical even pointed at itself, not the keeper). A 301 to the april file would be harmless if Clark ever wants belt-and-suspenders, but is unwarranted for a URL nothing could have surfaced. One commit, sync-push to GitHub only.

### Address correction - WRONG street number/suite fixed sitewide - SHIPPED 2026-06-11 (Clark corrected via William)
The prior reconciliation (commit `fb5cb25`) used **2601 PGA Blvd** with no suite - wrong street number and missing suite. Clark's confirmed canonical (matches OWYG corporate): **2401 PGA Blvd, Suite 164, Palm Beach Gardens, FL 33410**. `git fetch` first; local was even with origin/main, no admin-tool divergence. One logical commit, sync-push to GitHub only.
- [x] **Footer partial JSON-LD** (`partials/footer.html`, single source of truth): `streetAddress` "2601 PGA Blvd" -> **"2401 PGA Blvd, Suite 164"**; `geo` corrected from the 2601 block (26.8448/-80.0752) to the **2401 block, 26.8430 / -80.0738**. Locality/region/postal/country unchanged. Propagated to all 28 public pages via `sync-footer.sh` (28/28 updated).
- [x] **Displayed full-address strings** (6) changed "2601 PGA Blvd, ..." -> identical **"2401 PGA Blvd, Suite 164, Palm Beach Gardens, FL 33410"**: `contact.html`, `valuation.html`, `privacy.html`, `yachts/fringe-benefits.html`, `yachts/fortunato.html`, `yachts/riviera-4300-sports-express.html`.
- [x] **Verification:** repo-wide grep for "2601" returns ZERO matches. `streetAddress` collapses to one unique value ("2401 PGA Blvd, Suite 164") across all 29 files (28 pages + partial); displayed string collapses to one unique value. Email templates (`general.html`, `logbook.html`, issues, ray-pessel draft) already carried the 2401/Suite 164 address - now fully consistent with the site.
- Phone (561-817-1547) and business name untouched. OWYG (not OWYS) correct. Boat-berth/vessel-location mentions left alone (those are boats' locations, not the brokerage). Goes live on Clark's next cPanel pull.

### Pages / Content (technical)
- [x] **Publish the Riviera 4300 Sports Express spotlight article - DONE 2026-06-15 (Clark published it via the Article Manager).** Draft was saved in Article Manager format at `drafts/riviera-4300-sports-express-day-boat-bahamas-weekender.json` (original US-framed boat review by Patrick, approved by Clark; promotes the new 4300 arriving July, with an urgency CTA). Clark opened it in the Article Manager (Write mode), inserted the hero image, and published - which generated the page under `articles/boat-reviews/` and registered it in `articles/articles-data.js` automatically. Published via the API so it is live without a cPanel pull. Draft remains blocked from public via `drafts/.htaccess`.
- [x] **Riviera 4300 detail page - BUILT + PUBLISHED 2026-06-06.** `yachts/riviera-4300-sports-express.html` (modeled on the yachts/ listing template). Sections: hero, overview (Clark's copy), "From Clark Haley" note + lifestyle photo (image-03) with Inquire button below, click-to-load VIDEO + 360 facades (poster + play/launch, load embed on click), Model Features (6) + Technical Features (7) alternating strips, full Specifications table (imperial-first), and an all-images horizontal GALLERY strip (24 imgs) with prev/next arrow buttons. All 34 Riviera images downloaded into `images/yachts/riviera/` (lowercased; no remote hotlinks). Linked from Buy > New Yachts 4300 panel via a "View Details" link. The 4300 feature email button repointed to the detail page (utm_content=detail-page) in the preview - Clark sets that URL in Constant Contact. Pushed to GitHub main (commit 164b7a4); needs cPanel pull to go live. Evergreen (July-arrival wording removed per Clark). Riviera imagery used with permission; "courtesy of Riviera" credits in place; a heads-up to Riviera about featuring their media is still courteous to send.
  - **Riviera assets reviewed + VERIFIED for embedding (2026-06-06):**
    - VIDEO: official Riviera Australia YouTube "Riviera 4300 Sports Express", id `C50tIMSECBI`, embed `https://www.youtube.com/embed/C50tIMSECBI` (oEmbed-confirmed, embedding enabled). Use in a responsive 16:9 wrapper.
    - 360 WALKTHROUGH: official Riviera-hosted Pano2VR tour at `https://www.rivieraaustralia.com/virtual-tour/4300-sports-express-2025-05-31/` - returns HTTP 200, no X-Frame-Options / CSP, so iframe-embeddable. Reuse the existing `.listing-360` / `.listing-360-frame` pattern (same as the Kuula tours), just swap the src.
    - SPECS (factual, safe to restate) from the Riviera model page: LOA 13.88 m / 45 ft 6 in; beam 4.45 m / 14 ft 7 in; max draft 1.27 m / 4 ft 2 in; dry weight 15,498 kg; fuel 1,200 L / 316 gal; water 400 L / 105 gal; bridge clearance 4.67 m; sleeps 5; engines twin Volvo Penta D6 IPS 600 (440 hp ea) or IPS 650 (480 hp ea); top speed over 30 knots.
    - GALLERY (~21 Riviera promo images under rivieraaustralia.com/app/uploads/) + 4 magazine review PDFs: LINK OUT, do not re-host; these are other publishers' copyright. Get an explicit Riviera nod before featuring their photo set / video, and give Riviera a heads-up that we are iframing their 360 (it loads from their domain).
- [x] **OWYS -> OWYG cleanup (site-wide) - DONE 2026-06-15 (Clark approved).** Site-wide case-insensitive grep found 3 real company-name occurrences of "One Water Yacht Sales", all fixed to "One Water Yacht Group": `articles/_template.html` JSON-LD `worksFor.name` (L56) + author-card prose (L182), and `articles/boat-reviews/2026-05-04-world-premiere-riviera-6200-sport-yacht.html` JSON-LD `about.name` keyword string (L62). No other published article pages, JS, JSON, or data files contained the bad form. Re-grep after: zero "OWYS"/"One Water Yacht Sales" remain as a company name. Deliberately left untouched (rule/task references only, not branding): `CLAUDE.md` (the 'Never "OWYS"' rule), `docs/TASK-LIST.md` (this task's tracking lines), and this log line. On disk + pushed to GitHub main; needs cPanel pull to go live.
- [x] **Bahamas articles - remove inline Denison byline + inline photo (Clark's call).** Follow-up to the About-Clark photo fix (882825c). Clark decided to drop the original inline author/byline entirely and keep only the standardized "About Clark" card at the bottom + on the template. Removed from `articles/travel/2026-05-05-bahamas-cruising-permit-changes-again.html`: the inline `<figure class="article-figure float-left">` headshot (the one I had repointed earlier - Clark wanted it gone, not fixed) AND the inline byline `<p>` containing "(Formerly Denison Yachts)". Removed from `articles/travel/2026-05-05-bahamas-cruising-permit-changes-april-2026.html`: the same inline byline `<p>` (that file had no inline photo). Body paragraph above ("If you're shopping for a yacht...") kept on both; About Clark `<aside class="article-author-card">` kept intact on both. Markup clean at the seam - no empty container, stray wrapper, or orphaned caption. Site-wide grep for "Denison" (case-insensitive, whole tree): NOW ABSENT from all published/site-facing files. Only 3 internal/non-public hits remain, all expected: `images/video/cta-card/README.md` (a dev note stating the no-Denison rule), `docs/tasks/terry.md` (this log), `docs/tasks/patrick.md` (the no-Denison rule restated). None is a live page; left untouched. On disk + pushed to GitHub main; needs cPanel pull to go live.
- [x] Social icons (Facebook + Instagram): **BOTH LIVE on disk 2026-06-04. 100% sitewide, audited.** Inline-SVG FB + IG icons in the footer on every public page. Originally on 9 footers (7 main pages + `articles.html` + `articles/_template.html`); a 2026-06-04 audit found 15 real public pages built before the template update still lacked the block, so they were back-filled with the identical block: 13 article pages (boat-reviews x1, how-to x2, industry-news x6, newsletters x1, travel x3) plus the 2 yacht listing pages (`yachts/fortunato.html`, `yachts/fringe-benefits.html`). Block inserted in the identical footer spot (between `.footer-brand` and the copyright `<p>`); absolute hrefs + inline SVGs so directory depth is irrelevant. Final full-site audit: **24/24 public pages with a `site-footer` carry both FB + IG, 0 missing.** All FB anchors -> `https://facebook.com/clarkhaleyyachtbroker`, `data-social="facebook"`; all IG anchors -> `https://instagram.com/capnclark`, `data-social="instagram"`; aria-label, target="_blank", rel="noopener" in place. Plus a "Follow Along" block in the contact-page contact-info column. CSS: `.footer-social` + `.contact-social` in css/styles.css (brand-cyan #21cbea hover). Scope-guarded (correctly excluded): `email-templates/*`, `images/video/cta-card/cta-card-template.html`, admin tools. NOTE: changes are on disk only -> needs commit + push + cPanel pull to go live.
- [ ] Social icons - X and LinkedIn: still deferred. Only FB + IG requested 2026-06-04. Add later if Clark wants them (same pattern).

### Polish
- [ ] Final logo pass if Clark still wants to revisit
- [ ] Review color scheme / typography on any pages that feel off
- [x] Add `sitemap.xml` once a few articles are published - DONE 2026-06-10 (see Technical SEO batch above; sitemap.xml + robots.txt at root, commit `e2aac7f`).

### Pre-Launch Hardening
- [~] Confirm Formsubmit email verification is current for the contact form - **TEST SENT 2026-06-10, AWAITING CLARK'S ACTIVATION CLICK.** Endpoint confirmed: AJAX (`https://formsubmit.co/ajax/clark@HaleyYachts.com`), literal email (not a hashed token), method POST, `_subject` / `_captcha=false` / `_template=table` / `_honey` honeypot all present. Wiring was already correct on contact.html (form `action` and JS `fetch` both point at the AJAX endpoint). Fired one benign activation POST via curl to that AJAX endpoint with site `Origin`/`Referer` headers; Formsubmit returned `{"success":"true"}`. That `true` most likely means the address is ALREADY activated (the un-activated state returns a "confirm your email" message instead and withholds delivery), but Clark must check his inbox: if an activation email is present, click **Activate Form** in it. Activation email sender = "FormSubmit" / no-reply@formsubmit.co, subject ~ "Confirm your Email for FormSubmit" (a.k.a. "Activate Your Form").
- [~] Confirm Formsubmit email verification is current for the valuation form - **SAME TEST/STATE as contact form** (same destination address `clark@HaleyYachts.com`, so a single activation covers both). FIXED a wiring inconsistency 2026-06-10: valuation.html form `action` was the non-AJAX endpoint (`https://formsubmit.co/clark@HaleyYachts.com`) while its JS `fetch` posted to the AJAX endpoint; aligned the `action` to the AJAX endpoint so both files match and the no-JS fallback path is predictable. Awaiting Clark's activation click (see contact-form line for sender/subject/button).
- [x] Privacy policy PAGE: **BUILT + SHIPPED to repo 2026-06-06.** `privacy.html` at root, matches site design system (header/nav, `.page-header` hero, `.section`/`.container`, `.legal-content` prose styles added to css/styles.css). Full approved copy from `docs/drafts/privacy-policy-draft.md`. Resolved placeholders: Effective date + Last updated = June 6, 2026; children's-privacy age threshold = **13** (US COPPA; Clark may switch to 16 for EU). All contact points clark@haleyyachts.com / +1 561-817-1547 / 2601 PGA Blvd., West Palm Beach, FL 33410. No em dashes. Footer link added single-source in `partials/footer.html` (`.footer-links` -> `/privacy.html`), `privacy.html` added to the `sync-footer.sh` PAGES list, `sh scripts/sync-footer.sh` run clean (25/25, --check OK). NOTE: on disk + pushed to GitHub main; **needs cPanel git pull to go public.**
- [x] Cookie-consent banner: **CLOSED BY DECISION 2026-06-10.** Clark chose the browser/provider opt-out wording in the privacy policy instead of an on-site accept/decline banner (committed + pushed). No banner will be built. Revisit only if Haley Yachts ever targets EU / UK / EEA visitors, where GDPR would require an explicit consent gate.
- [ ] Mark GA4 conversion events as "key events" in the Analytics admin so they appear in Conversions reports (GA4 `G-6CVE0DG8Z3`)
- [ ] Future: per-listing inquiry forms, brochure/spec PDF downloads, gallery component - then wire the remaining 3 events Patrick scoped (listing_inquiry_submit, brochure_download, gallery_complete)
- [ ] Cross-browser test on Chrome, Safari, Firefox, Edge
- [ ] Mobile device test on iPhone and Android
- [ ] Revisit hero `min-height: 600px` safety floor during device testing

### Admin Tool Backlog
- [x] **Featured Yacht admin tool - DELETE function. BUILT 2026-06-08.** Added a "DELETE THIS YACHT" control to `admin/featured-yachts.html` (red `.btn-delete` under the MOVE block). Flow: load a Position, then Delete. It (1) removes that entry from `js/featured-yachts.js` via a new brace-aware `patchFeaturedYachtsDelete()` patcher (entry spliced out, array re-padded to 6 blank slots so positions stay fixed, renderer code untouched - same contract as the save/move patchers), committed via the Contents API (`ghPutFile` + read-back verify); and (2) deletes that entry's OWN assets - the `image:` file and the `pdf:` file (only when `pdf` is a local repo path, not an external https URL) - via `ghDeleteFile` (fetches each blob SHA then DELETEs). Works in both modes: GitHub token mode and the no-token local-folder fallback (new `deleteFileLocal()` via File System Access API). SAFETY: a `window.confirm()` summary names the yacht and lists exactly which files WILL be deleted vs KEPT; shared-asset guard (`countAssetRefs`) skips any image/PDF still referenced by another featured slot and reports it as kept; external-URL PDFs are never deleted. Detail pages (`page:` field) are NOT deleted this pass - the confirm dialog warns when a deleted card had a `page:`, since it can leave an orphan (see the card/detail-page sync item below). Version stamp bumped to `v: 2026-06-08 15:56 EDT`. Inline-JS parse-checked and the delete patcher unit-tested against the live data file (6 -> 6 entries, target removed, others + renderer intact). On disk + pushed to GitHub main; needs cPanel pull to go live.
  - **Follow-up flagged: orphaned detail pages.** Deleting a featured card that has a `page:` value leaves the `yachts/*.html` detail page in the repo (by design this pass - Clark asked for photos + PDF only). Today the only two `page:` entries are `yachts/fringe-benefits.html` and `yachts/fortunato.html`. Ties into the card/detail-page sync backlog item below; a future pass could offer to remove or unlink an orphaned detail page on delete.
- [x] **Image Library browser (with usage view) - BUILT 2026-06-10.** New standalone admin page `admin/image-library.html`, reachable from the Admin home (`admin/index.html`, new "Image Library" Live tool card) and linked from the insert flows of both the Article Manager (helper line under the body editor label) and the Featured Yacht editor (helper line under the Image Filename field). Reuses the shared `admin/js/github-api.js` plumbing (same token key / token box pattern as the other tools; read-only token is enough). TWO TABS: (1) **Browse & Copy Path** - recursively lists every image under `images/` and `articles/<cat>/images/` via the Contents API (`ghListDir`), renders a lazy-loaded thumbnail grid using each file's authenticated `download_url`, with a filename filter + per-folder filter; click any thumbnail to copy its site-relative repo path to the clipboard (Clipboard API with an execCommand fallback). (2) **Usage & Orphans** - fetches the text of all scannable source files (`.html/.js/.css`) under root + `css/js/yachts/partials/articles` (skips `admin/docs/drafts/documents/email-templates/social-media/.git`), then matches each image by BASENAME against that corpus (basename match is deliberate - article HTML references its own images as `images/foo.jpg` relative to `articles/<cat>/`, root pages use `images/...`, and some use `../../images/...`; basename is robust across all three). Builds a per-image filename-to-pages map, shows a summary (scanned / referenced / likely-unused counts), and lists "used on" files per image. Zero-reference images are flagged **"Likely unused - verify before deleting,"** sorted to the top, with a prominent BEST-EFFORT disclaimer (will not catch runtime-built names, external-CMS refs, or out-of-repo usage; never an auto-delete list). The data registries `articles/articles-data.js` and `js/featured-yachts.js` are inside the scan set, so registry-only hero references count as usage. No new server/plumbing - all client-side over the existing Contents API. Inline script parse-checked. On disk + committed + pushed to origin/main (both commits); needs cPanel pull to go live.
- [ ] **Featured Yacht admin tool - keep card and detail-page spec-sheet links in sync.** (Logged 2026-06-08 per Clark; LOW priority / not urgent - do NOT build yet, captured only.) The browser Featured Yacht editor (`admin/featured-yachts.html`) writes only `js/featured-yachts.js` (the card). Yacht detail pages (`yachts/*.html`) carry their own hardcoded spec-sheet link, so the two can silently drift. This caused the Fortunata stale-spec-sheet bug on 2026-06-08 (card showed the new PDF, detail page still served the old one). Fortunato detail page already fixed manually (commit d3bb27b). WHAT TO BUILD: when the admin tool sets/updates a yacht's PDF, it should also patch the matching spec-sheet link in that yacht's detail page - find the page via the yacht's `page:` field in `featured-yachts.js`, then find/replace the PDF href in that HTML. CLARK'S EXPLICIT REQUIREMENT: some featured yachts may NOT have a detail page; the code MUST check whether the detail page exists first, and if it does not, bypass the detail-page patch gracefully (no error, just update the card) - card-only featured yachts must still work. APPROACH: this is "option 2" from the 2026-06-08 analysis (patch both at the edit source, keeps detail pages static/self-contained, preserves the multi-page + JS-independent design Clark prefers). Rationale for low priority: only two detail pages exist today.

### MLS feed PULLED IN to display listings - IN PROGRESS / BLOCKED on OWYG feed access (REOPENED 2026-06-15 per Clark)
**Scope correction (Clark, 2026-06-15).** Two different things were conflated. Keep them separate:
- **Syndication OUT (pushing Haley listings to YachtWorld / boats.com / YATCO) = CLOSED.** One Water Yacht Group (OWYG) feeds those portals centrally; individual brokers have no control over that pipeline and there is nothing for us to build. This stays closed.
- **Feed IN (pulling an MLS feed INTO haleyyachts.com so the site can DISPLAY listings) = REOPENED, IN PROGRESS.** Clark now wants the site to render listings from a feed and is working with OWYG to obtain feed access/credentials. This is the live item.

**BLOCKED on Clark obtaining feed access/credentials from OWYG.** Nothing to build until the feed source exists.

**What I need from Clark / OWYG to start the build (once access is granted):**
- Feed TYPE: IDX, XML, or API? (Determines whether we poll/cache server-side, embed a provider widget, or call a JSON/REST endpoint client- or server-side.)
- Feed URL / endpoint to pull from.
- Credentials: API key, login, or token. (Keep out of the public repo - secrets do not go in git.)

**Open build questions to resolve with OWYG:**
- Data format / schema: field names, units, image URL structure, how specs and pricing are represented.
- Refresh cadence: how often the feed updates and how often we should pull/cache (live call vs scheduled cache).
- Scope: is it ALL Haley listings only, or a broader OWYG set we filter down to Haley?
- Render location on the site: the most likely home is the Buy page Worldwide tab, which currently shows the coming-soon panel (YachtSite embed was retired 2026-05-06). Confirm whether it replaces that panel and whether it becomes the default Buy tab.
- Note: hosting is GoDaddy Linux cPanel, so any server-side fetch/cache approach must work in that environment (PHP/cron available; no Node service unless Clark stands one up).

### Infra / Tooling
- [x] **Trial Leadfeeder - CLOSED / DROPPED 2026-06-15 (Clark's call).** Reverse-IP company identification is low value for a consumer brokerage (buyers are individuals/families, not companies with identifiable corporate IPs), so the trial is being skipped. No work to do.

---

## RECENTLY COMPLETED / DONE

### June 11
- [x] **Riviera 545 SUV broker co-broke email - FINALIZED 2026-06-11.** `email-templates/issues/riviera-545-suv-broker-cobroke.html` finalized (table-based, inline CSS, ESP-agnostic). Filled-per-send copy in `email-templates/issues/` per the standard issues workflow.

### June 6
- [x] **"About Clark" author photo fix (all articles) - quality + centering.** Clark reported the circular author photo at the bottom of every article was low quality and off-center. DIAGNOSIS: the 13 published article author cards loaded `images/people/clark-haley-headshot.jpg` (the 5504x8256, ~10.7 MB full-length studio portrait) downscaled to a 120px circle -> soft from extreme downscale, and because the source is a tall 2:3 portrait with `object-fit: cover` and NO `object-position`, the circle cropped the vertical center (chest area), not the face = the "off-center" symptom. The `_template.html` separately pointed at a nonexistent file `images/people/clark-headshot.jpg`. FIX: swapped all author cards to `images/people/clark-haley-headshot-square.jpg` - the purpose-built 600x600 face-framed crop already used as the canonical headshot in every email template; at 120px (240px @2x retina) it is sharp and self-centers in the circle. Updated 13 published articles + `_template.html` (14 files). CSS `css/styles.css` `.article-author-card .author-photo` gained `object-position: center center` as a safety. Also fixed a separate broken inline body image in `articles/travel/2026-05-05-bahamas-cruising-permit-changes-again.html` (`<img src="images/clark-haley-headshot.jpg">` resolved to a nonexistent path = broken on that page) -> repointed to the square crop with a real alt. No em dashes. On disk + pushed to GitHub main; needs cPanel pull to go live. FLAGGED to William for Clark: that same travel article's byline still reads "(Formerly Denison Yachts)" - left untouched (content/prose, and it touches the no-Denison rule) pending Clark's call.

### June 5
- [x] **Fortunato price reduction: $435,000 -> $395,000** - changed the asking price on every live, forward-facing surface. `yachts/fortunato.html` (5 spots: `<title>`, meta description, `og:title`, `.listing-price`, and the Asking spec row). `js/featured-yachts.js` (the Fortunato card `name`, which also feeds the homepage + buy.html featured grid). `social-media/cta-cards/render-360-cards.py` (2 `sub_line` strings for the 360 social cards). DELIBERATELY LEFT UNCHANGED: the dated May 2026 Logbook archives (`email-templates/issues/logbook-2026-05.html` and `articles/newsletters/2026-05-09-the-logbook-may-2026.html`) - those are sent/published historical records and reflect the price at send time; rewriting them would falsify a dated issue. No other yacht's price touched. On disk + committed; needs cPanel pull.
- [x] **"PRICE REDUCTION" overlay on the featured Southern Wind image** - `images/yachts/featured/southern-wind.jpg` (the image `js/featured-yachts.js` and `yachts/fortunato.html` use) now carries a bold red "PRICE REDUCTION" banner across the upper area with a black text stroke + a subtle dark backing band for legibility against the sky. Original aspect ratio preserved (4800x3200). Original backed up to `images/yachts/featured/southern-wind.original.jpg` (and tracked in git history). TOOLING NOTE: Clark's directive specified ImageMagick, but `magick`/`convert` is not installed on this box and there is no Homebrew to add it; used Python Pillow 11.3.0 (Arial Bold) to produce the equivalent overlay. If ImageMagick is wanted as the standard going forward, it needs installing. On disk + committed; needs cPanel pull.

### June 4
- [x] **Single-source-of-truth footer shipped** - the footer (brand logo, FB + IG social icons, copyright) now lives in ONE file, `partials/footer.html`, and is injected into all 24 public pages by `scripts/sync-footer.sh` between `<!-- FOOTER:START -->` / `<!-- FOOTER:END -->` markers. Future footer changes = edit the partial, run the script, commit, pull. Build-time injection (not client-side JS), so the footer stays in the static HTML and is fully crawlable for SEO; works with no JS and no server config. **Asset paths normalized to root-absolute** (`/images/brand/haleyyachtslogo.png`) so the same partial renders correctly at every depth (root, `articles/<cat>/`, `yachts/`) with no per-depth `../` juggling - verified the logo + links resolve at all three depths. Script is idempotent and has a `--check` mode (non-zero exit on drift) for pre-commit/CI verification. Pure refactor: rendered footer is byte-equivalent to before (same icons, same live hrefs FB `https://facebook.com/clarkhaleyyachtbroker` + IG `https://instagram.com/capnclark`, same copyright) - no visible change. Scope: 8 root + 13 `articles/` (incl. `articles/_template.html` so new articles inherit it) + 2 `yachts/`. Correctly excluded: `admin/`, `email-templates/`, `images/video/cta-card/cta-card-template.html`. Workflow documented in `docs/FOOTER-WORKFLOW.md`. Committed + pushed; needs cPanel pull to deploy.
- [x] **Social icons back-filled to 100% sitewide (15 pre-template pages)** - a full-site audit found that 15 real public pages built before the template update still lacked the `.footer-social` block: 13 article pages (boat-reviews x1, how-to x2, industry-news x6, newsletters x1, travel x3) plus both yacht listing pages (`yachts/fortunato.html`, `yachts/fringe-benefits.html`). Inserted the byte-identical block from index.html into each, in the identical footer spot (between `.footer-brand` and the copyright `<p>`). Absolute external hrefs + inline SVGs, so no `../` adjustment at any depth. Re-audited every public `.html`: **24/24 pages with a `site-footer` now carry both FB (`https://facebook.com/clarkhaleyyachtbroker`) and IG (`https://instagram.com/capnclark`), 0 missing.** Scope-guarded files correctly left untouched: `email-templates/*`, `images/video/cta-card/cta-card-template.html`, admin tools. On disk only; needs commit + push + cPanel pull.
- [x] **Social icons made truly sitewide** - the original staging missed two footers: `articles.html` (the articles index) and `articles/_template.html` (the template every new article is built from). Copied the exact `.footer-social` block from index.html into both, with live hrefs (FB `https://facebook.com/clarkhaleyyachtbroker`, IG `https://instagram.com/capnclark`) and matching `data-social` attributes. Block uses inline SVGs + absolute external hrefs, so no `../` path adjustment was needed at any depth; template's `../../css/styles.css` reference confirmed intact. Footer-social block now present on all 9 footers and byte-identical to index.html. Future articles built from the template inherit the icons automatically. On disk only; needs commit + push + cPanel pull.
- [x] **Instagram link activated** - Clark sent the IG URL (`https://instagram.com/capnclark`). All 8 Instagram anchors (footer on all 7 pages + the "Follow Along" block on contact.html) now point to `https://instagram.com/capnclark`, `data-social="instagram"`. The `instagram-pending` state is cleared site-wide and the leftover placeholder/TODO social comments are removed. Social-icons task is now COMPLETE pending deploy (FB + IG both live on disk). On disk only; needs commit + push + cPanel pull.
- [x] **Facebook link activated** - Clark sent the FB URL. All 8 Facebook anchors (footer on all 7 pages + the "Follow Along" block on contact.html) now point to `https://facebook.com/clarkhaleyyachtbroker`, `data-social="facebook"`. On disk only; needs commit + push + cPanel pull.
- [x] **Social icons staged (Facebook + Instagram)** - inline-SVG FB + IG icons added to the footer on all 7 pages plus a "Follow Along" block on contact.html. All hrefs were placeholders (`href="#"`, `data-social="*-pending"`, TODO comments). New `.footer-social` / `.contact-social` styles in css/styles.css with #21cbea hover. No fabricated URLs. (FB since activated - see above.)
- [x] **Article Manager upgrade** - committed + pushed to origin/main (commits `ad2d865`, `4b60029`, `69e8fad`, `437723b`). GitHub-API publish round-trip verified live (test article published then removed, `66b35af` -> `1a36ce2`), confirming Clark's PAT is in place and working.
  - New **Write Article** mode: in-browser rich text editor (ported from `email-composer.html`) with **insert image at cursor** and **automatic in-browser resize** (1600px longest edge, JPEG ~0.85; PNG kept only when it has transparency). Removes Clark's manual image pre-sizing step.
  - Existing **Upload Word Doc** path kept intact behind a mode toggle.
  - Storage/publish moved to the **GitHub Contents API** using a personal access token stored in the browser (localStorage). Publishes article HTML + images + patches `articles/articles-data.js`, all over the internet, so it works from any computer's browser with no local repo. Old File System Access (folder-picker) flow retained as a **transitional fallback** when no token is set.
  - **Drafts**: save / recall / search / delete under `drafts/` via the API. New `drafts/.htaccess` (`Require all denied`) blocks drafts from the public site.
  - "Manage Published Articles" load + remove also works via the API when a token is set.
  - Required article fields marked with red asterisk + legend (`69e8fad`); friendly token message on GitHub 401 (`437723b`); token controls guarded (`4b60029`).
  - **Folded-in fix**: the numbered-section `<ol>` bug - on the Word path, single-item heading-like `<ol>` blocks now convert to `<h2>`.
- [x] **Shared GitHub API publishing layer** - `admin/featured-yachts.html` + `admin/email-composer.html` migrated onto the same `admin/js/github-api.js` layer as the article manager (`d4dc2a3`).
- [x] **Remote-access setup runbook** added at `docs/REMOTE-ACCESS-SETUP.md` (iMessage channel + Remote Control, scoped to Monterey 12.7.6 box) (`61a6cf0`).
- [x] Article Manager: numbered-section rendering bug fixed. Each section heading in a Word doc got wrapped in its own single-item `<ol>`, so all sections rendered as "1." Real fix shipped in the June 4 upgrade: `fixWordSectionLists()` converts single-item heading-like `<ol>` blocks to `<h2>` on the Word path (write mode emits clean markup and is unaffected). The old manual `start="N"` workaround on the Bahamas how-to article can be left as-is or cleaned up on next edit.

> Reminder: all of the above is committed + pushed to GitHub. To go live on haleyyachts.com / haleymarine.com it still needs a **cPanel Git Version Control Pull** (manual step). Article-manager-published content goes live immediately via the API and does not need the pull.
>
> Site is LIVE at haleyyachts.com (and haleymarine.com - both share the same /public_html via alias). Deploys via GitHub -> cPanel Git Version Control Pull.

### May 26
- [x] Fortunato dedicated listing page shipped at `yachts/fortunato.html`, modeled on `yachts/fringe-benefits.html` + adds an inline "360 Walkthrough" section (Kuula collection 7MFLp embedded) above the gallery so social-campaign traffic lands directly on the walkthrough. Featured Yachts data: `page` field updated to `yachts/fortunato.html` so the buy.html grid's image + title now deep-link to the dedicated page (previously only the Inquire/360 buttons worked).

### May 25
- [x] Featured Yachts: restored lightbox/modal 360 UX on /buy. The previous save from `admin/featured-yachts.html` (when Clark added Fortunato's Kuula tour) had regenerated `js/featured-yachts.js` from the admin tool's older embedded template, clobbering the newer lightbox + hasPage code with the old inline-panel renderer. Restored on disk: full-screen modal opened by the 360 badge AND a separate "View 360 Tour" text button in card actions, image + title clickable to the dedicated `yachts/*.html` listing page when `page` is set (Fringe Benefits has one; Fortunato does not). Fortunato's `kuula:` URL preserved exactly.
- [x] Featured Yachts admin tool: root-cause fix. Save flow no longer regenerates `js/featured-yachts.js` from an embedded template. Instead it reads the current file, parses the `featuredYachts` array region with a brace-aware walker, and applies targeted field replacements to the chosen entry. Move-to-position rewrites only the array region. The renderer code, CSS, modal scaffold, and helper functions in `js/featured-yachts.js` are never touched - the admin tool can only mutate data, not behavior. This prevents the lightbox UX from being clobbered again the next time anyone edits a listing.

### May 4-6
- [x] articles.html filter UX - shipped 2026-05-06: chips toggle exclusion, single combined grid sorted newest-first, search overlay (5 single-select tabs replaced with 5 chip toggles + one combined date-sorted grid)
- [x] Remove `noindex, nofollow` meta tags from any pages that still have them - audited 2026-05-06: only correctly present on admin pages; bonus added missing tag to `admin/featured-yachts.html`
- [x] Add Google Analytics + Microsoft Clarity tracking - shipped 2026-05-06 via `js/analytics.js` (GA4 `G-6CVE0DG8Z3`, Clarity `wn3878tuvv`); 4 conversion events live (contact_form_submit, valuation_request_submit, phone_click, email_click) plus buy_engaged_view proxy at 90s
- [x] Site Analytics dashboard - shipped 2026-05-06 at `/admin/site-analytics.html`: 9 pre-filled deep links into GA4 + Clarity, GA4 Property ID input upgrades links to direct deep links once set
- [x] Favicon swap: new Haley Yachts brand monogram (rounded blue square + wave mark) replaces old compass-rose; multi-res ICO + 6 PNG sizes + SVG wrapper
- [x] Buy page: World Wide Listings broken yachtsite.com embed replaced with styled "coming soon" panel
- [x] Article Manager: fixed `removeArticle` calling undefined `buildArticlesDataFile`; now mirrors the proven regex-replace pattern from `updateManifest`
- [x] Featured Yacht admin: rewrote save flow to use site-root directory handle + read-back verification (the old per-file-handle approach was silently writing to wrong locations)
- [x] Featured Yacht admin: new "Move to Position" feature (insert-shift reorder of slots)

### May 12
- [x] Buy page: default tab switched from World Wide Listings to Featured Yachts pending MLS API source. World Wide Listings remains accessible (now 2nd tab) and `/buy.html#worldwide` deep link still works. Tabs reordered visually so the active tab is leftmost: Featured | World Wide | New.

### April 19
- [x] Active development migrated from old Mac to new Claude Code workspace; GitHub push verified from new machine
- [x] Internal docs (TASK-LIST, SITE-UPDATES) moved to `/docs/` subfolder with Apache deny rule so they no longer render at public URLs
- [x] `admin/.htaccess` synced with live server state (cPanel Directory Privacy `cp:ppd` block now tracked in git)
- [x] Authored fresh root `.htaccess`: force HTTPS + HSTS (Strict-Transport-Security), security headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy), extensionless URL routing, directory-listing + dotfile hardening

### April 17
- [x] GitHub repo published (captainclarkhaley/haleyyachts-website, public)
- [x] cPanel Git Version Control set up for one-step deploys from GitHub
- [x] Admin password protection activated via cPanel Directory Privacy
- [x] Riviera lineup on New Yachts tab: Sport Yachts, Sports Motor Yachts, SUVs, Sports Express - 18 models with images
- [x] 4300 Sports Express featured at top with 3-image panel and full description
- [x] Featured Yacht admin: renamed, expanded to 6 positions, empty-slot filtering, browse-and-copy image feature
- [x] Blog card images on About page (mvroam, double-wide)
- [x] Logo background made transparent
- [x] YachtSite embed auto-scroll issue fixed (space reservation + scroll guard)
- [x] Valuation banner added to bottom of Sell Your Yacht (sell.html) and Selling Assistance (services.html)
- [x] Hero video compressed to 13 MB (was 65 MB)

### Previously completed (technical)
- [x] Site architecture: multi-page HTML/CSS/JS, shared stylesheet, shared main.js
- [x] Hamburger drawer menu, header phone/email strip, enlarged logo, active nav highlighting
- [x] Sub-tab switching from URL hash
- [x] Logo, favicon, footer brand mark
- [x] About page: full bio, headshot, contact info, Get in Touch button, valuation banner
- [x] Contact page: headshot, Formsubmit form, phone validation, country selector
- [x] Valuation page auto-sends via Formsubmit
- [x] Mobile responsive layout
- [x] Home page hero video with poster fallback, refined headline/tagline
- [x] Buy page: Worldwide Listings (YachtSite embed), Featured Yachts, New Yachts tab
- [x] Sell page: Sell Your Yacht + Valuation tabs with imagery
- [x] Services page: Buying and Selling Assistance sections with images
- [x] Articles: template built, styles added, workflow documented
- [x] Em dashes removed site-wide (43 replacements) [build/markup pass]
- [x] Images folder reorganized by subject; filenames normalized
- [x] Featured Yachts section on Home page (shared data with Buy page)
- [x] Admin landing page + Article Manager + Content Manager + return nav
- [x] SITE-UPDATES.md reference doc

---

## NOTES / DECISIONS (technical)
- Hosting: GoDaddy Linux cPanel, primary domain is haleymarine.com with haleyyachts.com as alias (both share /public_html)
- Deploy workflow: edit -> GitHub Desktop commit + push -> cPanel Git Version Control Pull or Deploy tab -> Update from Remote
- GitHub repo is public; keep secrets out of the repo (use .gitignore for any API keys or credentials)
- DecapCMS available at /admin/content-manager.html but current workflow is Clark-sends-Claude-places
