# Haley Yachts - Task List (MASTER)

> **Auto-generated by William from `tasks/terry.md` + `tasks/patrick.md` - do not hand-edit; edit the owner files instead.**
>
> Single-writer rule: **Terry** writes only `tasks/terry.md`. **Patrick** writes only `tasks/patrick.md`. **William** owns and regenerates this master. Ask William to regenerate after either owner file changes.
>
> This is a concise digest of OPEN items only, ordered by priority. For full detail, history, and completed-work logs, see the two source files.
>
> See [`WISH-LIST.md`](WISH-LIST.md) for candidate features not yet committed (ideas to consider before building).

*Last regenerated: July 7, 2026 (William rollup). This pass folds in Terry's engineering-state reconciliation (commit `161b197`, origin/main): the Yacht Broker Support / Broker Suite platform work now leads the list. Top of the stack is the DKIM blocker for yachtbrokersupport.com (the Friday-send gate) and the Survey Management module go-live. Patrick's marketing items are unchanged from the June cadence. Added one STRATEGIC / PARKED item at the bottom: the YBS monetization direction (Clark's call, on hold).*

---

## PRIORITY 1 - THIS WEEK, BLOCKING (Terry / Clark)
Source of truth: [`tasks/terry.md`](tasks/terry.md)

### DKIM for yachtbrokersupport.com - THE blocker for real survey-email sends
Survey emails currently land in spam: no DKIM signature and DMARC is enforcing, so unsigned mail fails. The GoDaddy cPanel plan does NOT expose the "Email Deliverability" tool, so Terry cannot generate the key himself. Steps in `~/Dropbox/Yacht Broker Support - Change Logs/DKIM-fix-steps.txt`. (Separate from the older haleyyachts.com SPF/DKIM item - different domain / host.)
- Clark calls GoDaddy 2026-07-07 to enable DKIM / publish the `default._domainkey` TXT record.
- Verify live: DNS shows the record, then a real survey email in Gmail "Show original" reads DKIM: PASS and DMARC: PASS.
- Fallback ONLY if GoDaddy cannot enable it on this plan: route app mail through a transactional relay (SES / SendGrid / Postmark) that DKIM-signs on send.

### Survey Management module - test + go-live (built + iterated 2026-07-06, gated "coming soon")
New Broker Suite module in the `yacht-broker-support` repo (HEAD `88cfdc2`). Admin-previewable, NOT live to brokers. Real sends are blocked on DKIM above. To take it live:
- (a) Clark tests the module and signs off on the UI.
- (b) Flip `module_survey_state` to live for owyg in the Provider Console.
- (c) Write the Survey section of the in-app help manual (`help.php`) - HELD until Clark signs off on the UI so the copy matches the final screens.
- (d) Confirm brokers have a complete profile (name / cell / email) - the required-field check reads the profile, so an incomplete profile blocks a send.

---

## PRIORITY 2 - PARKED THIS WEEK (Terry / Clark)
Source of truth: [`tasks/terry.md`](tasks/terry.md)

### Vendor app onboarding go-live - CODE-COMPLETE, needs a live E2E test
Onboarding email + forced-password-change-on-first-login are code-complete and verified. Real delivery depends on the DKIM/SPF work above.
- Live end-to-end test: create a staff account, receive the onboarding email, first login forces the password change, confirm the flow.
- Vendor-documents expiry-reminder cron needs a SECOND cPanel cron registered by Clark (the reminder does not run until scheduled).

### Console stats cron (ybs-provider) - never registered in cPanel
The daily stats cron (`app/cron/stats-cron.php`) was never registered, which is why usage stats / trend charts were not refreshing.
- Clark adds the daily cron entry in cPanel for `app/cron/stats-cron.php`.
- Fix the deploy runbook to include registering this cron (and the vendor-documents reminder cron) so it is not missed on future deploys.

### Survey Management - optional follow-ups (offered, NOT built - only if Clark wants them)
- Per-survey broker-contact override (contact currently always pulled from the broker profile).
- Inline "create a brand-new vendor type" from within the survey flow.
- Duplicate-vendor-by-email warning.

---

## PRIORITY 3 - MARKETING (Patrick)
Source of truth: [`tasks/patrick.md`](tasks/patrick.md)

- **Local citations / NAP consistency** - plan DELIVERED at `docs/marketing/local-citations-plan.md`; 18 targets, content staged, awaiting Clark to execute (account / verification gated).
- **GBP seasoning** - 4 Google Posts + 8 starter Q&A packaged as copy-paste blocks + how-to-post steps (source: Sections 8-9 of the GBP setup doc); pending Clark to publish + add monthly photos.
- **Articles: 8 approved JSON drafts** queued in `drafts/` for Clark to review + publish (Topics 2, 3, 4, 6, 7, 8, 9, 10). Source outlines: `docs/drafts/article-topics.md`.
- **The Logbook newsletter** - per-issue cadence ongoing (master template built). June issue was Clark-built; keep the monthly rhythm.
- **Buyer-keyword article cadence** - establish an ongoing buyer-intent article cadence (ties into Patrick-writes -> Article Manager -> Clark-publishes).
- **Backlinks** - pursue quality inbound links (IYBA, OWYG corporate site, local Palm Beach Gardens / Jupiter business + marine sites, PR).
- Article CONTENT pipeline (words; Terry owns the tool).
- Brand-voice copy refinements (ongoing).
- Listing / positioning copy as new listings arrive (e.g. the Offshore 48 "Island Girl" copy is delivered and awaiting specs from Clark).
- **Contact-page + footer social icons** - FB + IG decided; markup staged by Terry, BLOCKED on Clark supplying the real FB + IG URLs.
- **Privacy policy** - Patrick's draft is Clark-approved copy; Terry has shipped `privacy.html` + footer link (live pending Clark's cPanel pull).

---

## PRIORITY 4 - BACKLOG (low / later)
Source of truth: [`tasks/terry.md`](tasks/terry.md)

- **Shrink the Broker Suite launcher tiles** so all module cards (Survey, Vendor, Pocket, Broker Requests, View Brokers) fit a laptop screen without vertical scrolling. Pure CSS/layout, no functional change; keep responsive behavior (phone stacked, desktop grid). Clark raised it 2026-07-06 as "just a thought to look at later."

---

## PRIORITY 5 - STRATEGIC / PARKED (Clark's call)
Not owned by Terry or Patrick - a direction decision for Clark, on hold.

- **YBS monetization direction.** Before more build investment in Yacht Broker Support, Clark wants to (a) define what to ADD to the product, and (b) research willingness to pay / pricing. On hold pending Clark's decision. This gates how much further engineering effort goes into the platform.

---

## Status
- **Yacht Broker Support / Broker Suite** is the active engineering front (repos `yacht-broker-support` + `ybs-provider`; hosted on its own GoDaddy cPanel, git-pull deploy). Survey module built + gated; go-live blocked on DKIM.
- **haleyyachts.com** is LIVE (and haleymarine.com via shared /public_html alias). Deploys via GitHub -> cPanel Git Version Control Pull; Article-Manager-published content goes live immediately via the GitHub API.
- Durable session record: `docs/session-log.md`. For completed-work history, see the "Recently completed / Done" sections in each owner file.
