# Mac system files
.DS_Store
Thumbs.db

# Backup and build archives
*.zip

# Large media (GitHub blocks files >100MB). Keep raw video out of the repo;
# publish hosted versions (Vimeo/YouTube/Cloudinary) and embed by URL.
*.mp4
*.mov
*.m4v

# Dev preview files
logo-preview.html

# Temp/misc files
ziuSKY1z

# Editor/IDE files
.vscode/
.idea/
*.swp

# Logs
*.log

# Future: secrets and env files - never commit these
.env
.env.local
*.key
credentials.json

# HAVEN print artwork: regenerate with scripts/build-haven-print.py.
# The PDF carries ~9 MB of embedded photography and is rebuilt on demand.
docs/design/build/
# Where the finished CMYK file for the publisher lands. 43 MB of lossless
# photography, rebuilt by the same script; it is a deliverable, not source.
docs/design/print-ready/
